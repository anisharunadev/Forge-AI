---
plan: 0.5-02
phase: 0.5
phase_name: UI Foundation
wave: 2
depends_on: [0.5-01]
gap_closure: false
autonomous: true
files_modified:
  - apps/forge/components/ui/badge.tsx
  - apps/forge/components/ui/skeleton.tsx
  - apps/forge/components/ui/checkbox.tsx
  - apps/forge/components/ui/alert.tsx
  - apps/forge/components/ui/popover.tsx
  - apps/forge/components/ui/table.tsx
  - apps/forge/components/ui/command.tsx
  - apps/forge/components/shell/StatusPill.tsx
  - apps/forge/app/error.tsx
  - apps/forge/app/loading.tsx
  - apps/forge/app/global-error.tsx
  - apps/forge/app/not-found.tsx
  - apps/forge/components/RunStatusBadge.tsx
  - apps/forge/components/connector-center/HealthBadge.tsx
  - apps/forge/components/architecture/ApprovalStatusBadge.tsx
  - apps/forge/components/ideation/ScoreBadge.tsx
  - apps/forge/components/knowledge-center/FreshnessBadge.tsx
  - apps/forge/components/validator/SeverityBadge.tsx
  - apps/forge/components/ConnectorStatusPill.tsx
requirements: [PILOT-01, PILOT-05, PILOT-09]
---

<objective>
Land the StatusPill primitive (the single source of truth for state-bearing chips), install the missing shadcn primitives the rest of phase 0.5 depends on, and add the three Next.js error/loading boundaries the app currently lacks. Migrate the 7 bespoke status badges + ~10 inline STATUS_TONE records to thin wrappers around StatusPill so a brand refresh propagates through one component.

This plan does NOT redesign any center page or build the new shell. It establishes the primitives that Plans 0.5-03..06 compose against.

Source spec: curated design vision (Phase 0.5 amendment, 2026-06-23) §6 Agent Status Language — color + glyph + pulse + label, never color alone.

</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/references/ui-brand.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/0.5-ui-foundation/0.5-01-PLAN.md (does not exist; refer to commit history and apps/forge/lib/design-system/)
@app/forge/lib/design-system/forge-color-tokens.ts (committed)
@app/forge/lib/design-system/status.ts (committed — StatusTone, StateGlyph, PulseKind, agentStateToTone, agentStateGlyph, agentStatePulse, toneClasses)
@app/forge/tailwind.config.ts (committed — semantic colors, type scale, radius, motion)
@app/forge/app/globals.css (committed — token blocks)
@app/forge/app/layout.tsx (committed — ThemeProvider, next/font, skip-link)
@app/forge/.eslintrc.json (committed — no-restricted-syntax warn)
</context>

<must_haves>
truths:
  - "`components/shell/StatusPill.tsx` exists and accepts `tone` (StatusTone), `glyph` (StateGlyph), `pulse` (PulseKind), `label` (string), and a `size` prop"
  - "StatusPill renders color + glyph + label (no color-only states) per the curated spec"
  - "The 7 bespoke badge files (RunStatusBadge, HealthBadge, ApprovalStatusBadge, ScoreBadge, FreshnessBadge, SeverityBadge, ConnectorStatusPill) become thin wrappers around StatusPill, keeping their original prop API for back-compat"
  - "apps/forge/app/error.tsx, loading.tsx, and global-error.tsx exist and use the new shell styling"
  - "apps/forge/app/not-found.tsx is updated to use the new shell + an EmptyState primitive (may be inline if EmptyState is not yet extracted — 0.5-03 owns that)"
  - "The 4 most-needed missing shadcn primitives are present in components/ui/: skeleton, checkbox, alert, popover"
  - "The existing 13 shadcn primitives (button, card, dialog, etc.) are unchanged in API; only the new primitives are added"
  - "All 6 toneClasses from status.ts (success/warn/danger/info/idle/agent/execution/review/cost) are reachable from StatusPill"
  - "Zero `any` regressions in TypeScript; new files are strict"
  - "Vitest unit tests for StatusPill exist (one per tone variant) and pass"
artifacts:
  - path: "apps/forge/components/shell/StatusPill.tsx"
    provides: "Single source of truth for state-bearing chips (color + glyph + pulse + label)"
  - path: "apps/forge/components/ui/skeleton.tsx"
    provides: "shadcn Skeleton primitive for loading placeholders"
  - path: "apps/forge/components/ui/checkbox.tsx"
    provides: "shadcn Checkbox primitive (replaces 2 raw <input type=checkbox> in StepTenantSetup)"
  - path: "apps/forge/components/ui/alert.tsx"
    provides: "shadcn Alert primitive (replaces ad-hoc colored callouts in governance-center, etc.)"
  - path: "apps/forge/components/ui/popover.tsx"
    provides: "shadcn Popover primitive (used by DataToolbar in 0.5-04)"
  - path: "apps/forge/app/error.tsx"
    provides: "Per-route error boundary using new shell"
  - path: "apps/forge/app/loading.tsx"
    provides: "Streaming loading fallback using new shell"
  - path: "apps/forge/app/global-error.tsx"
    provides: "Root error boundary (replaces Next.js default error page)"
</must_haves>

<tasks>

<task type="auto">
  <name>Task 1: Run shadcn add for missing primitives</name>
  <read_first>
    /home/arunachalam.v@knackforge.com/forge-ai/apps/forge/components.json
    /home/arunachalam.v@knackforge.com/forge-ai/apps/forge/components/ui/badge.tsx
  </read_first>
  <files>apps/forge/components/ui/{skeleton,checkbox,alert,popover}.tsx</files>
  <action>
    From apps/forge/, run: `pnpm dlx shadcn@latest add skeleton checkbox alert popover --yes`

    Verify all four files land in `apps/forge/components/ui/`. Do NOT modify the existing 13 primitives (button, card, dialog, dropdown-menu, input, label, scroll-area, select, separator, sheet, tabs, textarea, toast, toaster, tooltip, command, badge). Do NOT add table/pagination/breadcrumb/avatar/accordion/form in this plan — those are 0.5-04's job.

    If `shadcn add` fails (offline, registry 404), author the four primitives by hand following the shadcn new-york pattern. Use `components/ui/badge.tsx` as the structural template; replace the underlying Radix primitive (Skeleton has no Radix dep; Checkbox uses `@radix-ui/react-checkbox` which needs `pnpm add`; Alert uses class-variance-authority; Popover uses `@radix-ui/react-popover` which also needs `pnpm add`).
  </action>
  <verify>
    - `ls apps/forge/components/ui/{skeleton,checkbox,alert,popover}.tsx` shows all four files
    - `pnpm typecheck` does not regress (pre-existing test errors in ideation-approval-decide.test.tsx are out of scope)
  </verify>
  <done>Four files present; typecheck shows no new errors</done>
</task>

<task type="auto">
  <name>Task 2: Build StatusPill primitive</name>
  <read_first>
    /home/arunachalam.v@knackforge.com/forge-ai/apps/forge/lib/design-system/status.ts
    /home/arunachalam.v@knackforge.com/forge-ai/apps/forge/lib/design-system/forge-color-tokens.ts
    /home/arunachalam.v@knackforge.com/forge-ai/apps/forge/components/ui/badge.tsx
  </read_first>
  <files>apps/forge/components/shell/StatusPill.tsx</files>
  <action>
    Create `apps/forge/components/shell/StatusPill.tsx` (a new directory; create the directory as needed).

    Component contract:
    ```ts
    interface StatusPillProps {
      tone: StatusTone       // from lib/design-system/status
      glyph?: StateGlyph     // '○' | '◐' | '●' | '◑' | '✓' | '✕' (or auto-derived from tone)
      pulse?: PulseKind      // 'none' | 'slow' | 'active' | 'fast-to-static'
      label: string          // always shown — color is never the only signal
      size?: 'sm' | 'md'     // sm = h-5 text-[11px], md = h-6 text-xs
      className?: string
    }
    ```

    Implementation:
    - Use the existing `toneClasses[ tone ]` map from `lib/design-system/status.ts` for bg/fg/ring
    - Map `pulse` to a Tailwind `animate-*` class:
      - `'none'` → no animation
      - `'slow'` → `animate-pulse-agent` (1.6s)
      - `'active'` → `animate-spin-execution` (1.2s)
      - `'fast-to-static'` → a one-shot pulse (use `animate-pulse` with `animation-iteration-count: 1` via inline style, or document a TODO for a custom keyframe)
    - Default radius: `rounded-md` (8px, per the curated spec — Cards 8px)
    - Include the `data-testid="status-pill"` and `aria-label={label}` for a11y/QA
    - Honor `prefers-reduced-motion` by collapsing to no animation (CSS handles this via globals.css; do not duplicate)

    After creating, also create `apps/forge/components/shell/index.ts` that barrel-exports `StatusPill`.

    Add a Vitest test at `apps/forge/tests/shell/status-pill.test.tsx` with one assertion per tone (renders correct label, correct classes). Keep it under 80 lines.
  </action>
  <verify>
    - File exists
    - `pnpm test shell/status-pill` passes the 9 tone variants
    - `pnpm typecheck` no new errors
  </verify>
  <done>StatusPill primitive exists with full API + 9 tone tests passing</done>
</task>

<task type="auto">
  <name>Task 3: Migrate 7 bespoke badges to StatusPill wrappers</name>
  <read_first>
    /home/arunachalam.v@knackforge.com/forge-ai/apps/forge/components/shell/StatusPill.tsx
    /home/arunachalam.v@knackforge.com/forge-ai/apps/forge/components/RunStatusBadge.tsx
    /home/arunachalam.v@knackforge.com/forge-ai/apps/forge/components/connector-center/HealthBadge.tsx
    /home/arunachalam.v@knackforge.com/forge-ai/apps/forge/components/architecture/ApprovalStatusBadge.tsx
    /home/arunachalam.v@knackforge.com/forge-ai/apps/forge/components/ideation/ScoreBadge.tsx
    /home/arunachalam.v@knackforge.com/forge-ai/apps/forge/components/knowledge-center/FreshnessBadge.tsx
    /home/arunachalam.v@knackforge.com/forge-ai/apps/forge/components/validator/SeverityBadge.tsx
    /home/arunachalam.v@knackforge.com/forge-ai/apps/forge/components/ConnectorStatusPill.tsx
  </read_first>
  <files>
    apps/forge/components/RunStatusBadge.tsx
    apps/forge/components/connector-center/HealthBadge.tsx
    apps/forge/components/architecture/ApprovalStatusBadge.tsx
    apps/forge/components/ideation/ScoreBadge.tsx
    apps/forge/components/knowledge-center/FreshnessBadge.tsx
    apps/forge/components/validator/SeverityBadge.tsx
    apps/forge/components/ConnectorStatusPill.tsx
  </files>
  <action>
    For each of the 7 files, refactor to a thin wrapper around StatusPill. Preserve the existing prop API (so callsites do not change), but delegate rendering to StatusPill.

    Mapping rules (state → tone, derived from the existing STATUS_TONE / STATUS_LABEL records in each file):
    - `RunStatusBadge`: existing status keys (running/waiting_approval/approved/rejected/returned/created/paused/aborted/finished/done) → use `runStateTone` from `lib/design-system/status.ts`. Add a glyph per state: running = '●', waiting_approval = '◑', approved = '✓', rejected = '✕', returned = '◑', created = '○', paused = '●', aborted = '✕', finished = '✓', done = '✓'. Pulse: running = 'active', waiting_approval = 'slow', thinking = 'slow', etc.
    - `HealthBadge`: healthy → 'success' ✓, degraded → 'warn' ◑, down → 'danger' ✕, unknown → 'idle' ○
    - `ApprovalStatusBadge`: approved → 'success' ✓, rejected → 'danger' ✕, pending → 'review' ◑
    - `ScoreBadge`: high → 'success' ✓, medium → 'review' ◑, low → 'danger' ✕
    - `FreshnessBadge`: fresh → 'success' ✓, stale → 'warn' ◑, expired → 'danger' ✕
    - `SeverityBadge`: critical → 'danger' ✕, high → 'danger' ✕, medium → 'review' ◑, low → 'info' ○, info → 'idle' ○
    - `ConnectorStatusPill`: connected → 'success' ✓, error → 'danger' ✕, syncing → 'execution' ●, idle → 'idle' ○

    Each wrapper:
    - Imports `StatusPill` from `@/components/shell`
    - Imports the relevant tone/glyph/pulse map from `@/lib/design-system/status` (or a local one if more specific)
    - Returns `<StatusPill tone={…} glyph={…} pulse={…} label={…} />`
    - Keeps the same default props the original component had (size, className passthrough)

    After migration, grep the codebase for `bg-emerald-500/15`, `bg-amber-500/15`, `bg-rose-500/15`, `bg-violet-500/15` in the 7 files. They should be 0 (the wrappers now read from CSS variables via toneClasses).
  </action>
  <verify>
    - All 7 files import StatusPill
    - All 7 files no longer contain `bg-emerald-500/15`, `bg-amber-500/15`, `bg-rose-500/15`, `bg-violet-500/15`, `bg-forge-500/15`
    - `pnpm typecheck` no new errors
    - Existing component tests (`apps/forge/tests/connector-card.test.tsx`, etc.) still pass
  </verify>
  <done>7 badges delegate to StatusPill; no inline emerald/amber/rose/violet status classes remain in the 7 files</done>
</task>

<task type="auto">
  <name>Task 4: Add app/error.tsx, loading.tsx, global-error.tsx</name>
  <read_first>
    /home/arunachalam.v@knackforge.com/forge-ai/apps/forge/app/not-found.tsx
    /home/arunachalam.v@knackforge.com/forge-ai/apps/forge/components/BackendBanner.tsx
  </read_first>
  <files>
    apps/forge/app/error.tsx
    apps/forge/app/loading.tsx
    apps/forge/app/global-error.tsx
    apps/forge/app/not-found.tsx
  </files>
  <action>
    Create three Next.js error/loading boundary files using the new shell styling (semantic tokens, NOT `.card` utility or `forge-*` classes).

    **`app/error.tsx`** — client component, `'use client'`. Accepts `{ error, reset }`. Renders:
    - Centered column with `text-foreground` text
    - Eyebrow: `text-2xs uppercase tracking-wider text-subtle` "Something went wrong"
    - H1: `text-24` "We hit an unexpected error"
    - Body: `text-14 text-muted-foreground` showing the error message (truncated to 240 chars)
    - Primary button `<Button>` calling `reset()` "Try again"
    - Secondary button `<Button variant="outline">` linking to `/dashboard` "Back to dashboard"
    - data-testid="app-error"

    **`app/loading.tsx`** — server component (no `'use client'`). Renders a centered Skeleton-based loading state:
    - 4 placeholder rows in a vertical stack
    - Each row: `Skeleton className="h-4 w-3/4"` then `Skeleton className="h-4 w-1/2 mt-2"`
    - Wrapped in `mx-auto max-w-3xl py-16` to match the `max-w-7xl` shell
    - data-testid="app-loading"

    **`app/global-error.tsx`** — must be its own `<html><body>` because it replaces the root layout on catastrophic failure. Renders:
    - Minimal centered HTML
    - Same eyebrow / h1 / body / button structure as `error.tsx`
    - Inline `<style>` block (since globals.css is not guaranteed to load) for the dark background

    **Update `app/not-found.tsx`** to use the same shell (replace any `.card` usage with shadcn `Card` + tokens; add a Back to dashboard button).
  </action>
  <verify>
    - `pnpm typecheck` no new errors
    - `pnpm dev` and `curl -s localhost:3000/some-nonexistent-path` returns the not-found page (HTTP 404)
    - The four files all use semantic tokens (no `.card`, no `forge-*` literal classes)
  </verify>
  <done>Three boundaries exist; not-found.tsx uses the new shell; typecheck clean</done>
</task>

<task type="auto">
  <name>Task 5: Commit + update STATE.md + verify</name>
  <read_first>
    /home/arunachalam.v@knackforge.com/forge-ai/.planning/STATE.md
  </read_first>
  <files>
    .planning/STATE.md
  </files>
  <action>
    1. Commit all changes as `feat(forge): Phase 0.5-02 — StatusPill primitive + 7 badge migrations + error/loading boundaries` with a multi-paragraph body listing the new files and the migration mapping.
    2. Update `.planning/STATE.md`:
       - `current_phase: 0.5` (was `0`)
       - `current_phase_name: UI Foundation`
       - Add to the `By Phase` table: `| 0.5. UI Foundation | 0 | 7 | — |` (then bump to 1 after this plan merges)
       - Add a "Recent decisions affecting current work" entry: "Phase 0.5-02: StatusPill is the single source of truth for state-bearing chips; 7 bespoke badges delegate to it (PILOT-01/05/09 alignment)."
       - Update `last_activity` and `last_activity_desc`
    3. Run `pnpm test` and `pnpm typecheck` to confirm no regressions beyond the pre-existing `tests/intelligence/ideation-approval-decide.test.tsx` errors (out of scope, do not fix).
  </action>
  <verify>
    - `git log --oneline -1` shows the new commit
    - `pnpm test` runs the new StatusPill tests; existing component tests still pass
    - `pnpm typecheck` shows the same pre-existing errors but no new ones
  </verify>
  <done>Commit + STATE.md updated + tests pass</done>
</task>

</tasks>

<verification>
End-to-end:
1. `pnpm dev` boots without errors
2. Open `/dashboard` — no visual regression (existing 13 e2e tests still pass via `pnpm test:e2e`)
3. Open any page that uses one of the 7 migrated badges (e.g., `/connector-center` for HealthBadge, `/audit` for RunStatusBadge, `/architecture` for ApprovalStatusBadge) — the badge renders with the new color/glyph/label triple, never color alone
4. Force an error in any client component (e.g., `throw new Error('test')`) — the new `error.tsx` boundary renders, NOT the Next.js default error page
5. Navigate to a non-existent route — the new `not-found.tsx` renders
6. `pnpm test` — StatusPill tests pass; existing tests still pass
7. `pnpm typecheck` — no new errors
</verification>

<output>
After completion, update STATE.md, commit, and report:
- 1 commit on main branch: "feat(forge): Phase 0.5-02 — StatusPill primitive + 7 badge migrations + error/loading boundaries"
- 4 new shadcn primitives in components/ui/ (skeleton, checkbox, alert, popover)
- 1 new primitive in components/shell/ (StatusPill + index barrel)
- 1 new Vitest test file (tests/shell/status-pill.test.tsx)
- 3 new app/ boundary files (error, loading, global-error)
- 1 updated app/not-found.tsx
- 7 refactored badge files (now thin wrappers around StatusPill)
- STATE.md updated to current_phase: 0.5
</output>
