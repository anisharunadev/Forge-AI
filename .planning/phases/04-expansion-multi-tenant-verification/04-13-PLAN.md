---
phase: "04"
plan_id: "04-13"
title: "F17 Background Responses — /api/forge/responses routes + 24h retention + cancel-within-100ms"
wave: 3
depends_on: ["04-10"]
files_modified:
  - backend/app/api/v1/forge_phase4/sessions.py
  - backend/app/services/phase4_sessions.py
  - backend/app/db/models/phase4.py
  - backend/app/core/phase4_audit_events.py
  - backend/app/services/scheduler/responses_cleanup.py
  - backend/tests/phase4/test_background_responses.py
autonomous: true
requirements_addressed:
  - SCALE-F17-AC4
  - SCALE-F17-AC6
  - SCALE-F17-AC8
must_haves:
  truths:
    - "POST /api/forge/responses creates a background response (session type=background, max_duration=24h)."
    - "GET /api/forge/responses/{id} returns the response if still pollable; if past expires_at, returns 410 Gone with status='expired'."
    - "POST /api/forge/responses/{id}/cancel cancels within 100ms (same as session cancel — Redis pub/sub propagation)."
    - "POST /api/forge/responses/{id}/input_items appends input items to the response stream."
    - "GET /api/forge/responses/{id}/input_items lists the input items."
    - "POST /api/forge/responses/{id}/compact reduces token count by > 50% (covered in 04-11 but verified here for background type)."
    - "Daily cron deletes background response rows where expires_at < now() - 24h (i.e., already expired for >24h)."
    - "test_background_response_pollable_at_24h passes: with mocked time advancement of 23h59m, response is still pollable; at 24h01m, returns 410 Gone."
  prohibitions:
    - "MUST NOT retain background responses indefinitely — 24h max from creation."
    - "MUST NOT allow cancel to take longer than 100ms (Redis pub/sub propagation already enforced in 04-10)."
    - "MUST NOT skip the audit row on cancel/expire (R6 mandatory)."
---

<objective>
Ship the background response endpoints (/api/forge/responses/{id,id/cancel,id/stream,id/input_items,compact}) with 24-hour retention, cancel-within-100ms semantics, and a cleanup cron that hard-deletes expired responses after a 24h grace period.
</objective>

<context>
@.planning/phases/04-expansion-multi-tenant-verification/00-source-spec.md (Feature 17 AC4, AC6, AC8)
@.planning/phases/04-expansion-multi-tenant-verification/01-CONTEXT.md (L5)
@.planning/phases/04-expansion-multi-tenant-verification/04-RESEARCH.md (Session State Architecture)

@backend/app/api/v1/forge_phase4/sessions.py (after 04-10, 04-11)
@backend/app/services/phase4_sessions.py (after 04-10)
@backend/app/db/models/phase4.py::Phase4Session, Phase4SessionEvent
@backend/app/services/scheduler/ (existing cron pattern)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Background response endpoints + 24h retention cron + retention test</name>
  <files>backend/app/api/v1/forge_phase4/sessions.py, backend/app/services/phase4_sessions.py, backend/app/services/scheduler/responses_cleanup.py, backend/tests/phase4/test_background_responses.py</files>
  <read_first>
    - backend/app/api/v1/forge_phase4/sessions.py (after 04-11)
    - backend/app/services/phase4_sessions.py (after 04-10)
    - backend/app/services/scheduler/ (existing cron pattern)
  </read_first>
  <acceptance_criteria>
    - POST /api/forge/responses with body {prompt, model} returns 200 + {id: <uuid v7>, status: 'active', expires_at: <iso 24h from now>}.
    - GET /api/forge/responses/{id} returns the response within 24h; returns 410 Gone + {status: 'expired'} after 24h.
    - POST /api/forge/responses/{id}/cancel returns 200 within 100ms; status='cancelled'.
    - POST /api/forge/responses/{id}/input_items with body [{role: 'user', content: '...'}] appends items.
    - GET /api/forge/responses/{id}/input_items returns the list of input items.
    - test_background_response_pollable_at_24h passes with mocked time.
    - Daily cron hard-deletes Phase4Session rows WHERE type='background' AND status='expired' AND expires_at < now() - INTERVAL '24 hours'.
    - All endpoints require require_permission("forge.responses.read") for GET, require_permission("forge.responses.write") for POST.
  </acceptance_criteria>
  <action>
    In backend/app/api/v1/forge_phase4/sessions.py, add routes: POST /responses (creates session type='background'), GET /responses/{id} (returns 410 if status='expired' or expires_at < now()), POST /responses/{id}/cancel (calls phase4_sessions.cancel — same 100ms propagation), POST /responses/{id}/input_items (appends to Phase4SessionEvent with event_type='input_item'), GET /responses/{id}/input_items. In backend/app/services/phase4_sessions.py, add methods: get_response(id, tenant_id, project_id) that returns the session OR raises SessionExpired; append_input_items(id, tenant_id, project_id, items) that INSERTs Phase4SessionEvent rows. Create backend/app/services/scheduler/responses_cleanup.py with async function daily_cleanup() that DELETEs Phase4Session rows WHERE type='background' AND status='expired' AND expires_at < now() - INTERVAL '24 hours'; register with APScheduler at 03:00 UTC. In backend/tests/phase4/test_background_responses.py, write test_background_response_pollable_at_24h using mocked datetime.
  </action>
  <verify>
    ```bash
    cd /home/arunachalam.v@knackforge.com/forge-ai/backend
    pytest tests/phase4/test_background_responses.py::test_background_response_pollable_at_24h -x 2>&1 | tail -10
    pytest tests/phase4/test_background_responses.py::test_cancel_within_100ms -x 2>&1 | tail -5
    pytest tests/phase4/test_background_responses.py::test_daily_cleanup_hard_deletes -x 2>&1 | tail -5
    ```
  </verify>
  <done>Background responses live; 24h retention enforced; cleanup cron works; cancel propagates within 100ms.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Background response row to user | 24h retention — past expires_at, row is gone or status='expired' |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-13-01 | Information Disclosure | background response leaks after 24h | medium | mitigate | Cleanup cron hard-deletes after 24h grace |
| T-04-13-02 | Denial of Service | cancel slow | high | mitigate | Redis pub/sub same as session cancel; 100ms SLA |

</threat_model>

<verification>
- pytest backend/tests/phase4/test_background_responses.py exits 0
</verification>

<success_criteria>
- One task committed.
- 04-13-SUMMARY.md written.
- F17 AC4, AC6, AC8 all have at least one passing test.
</success_criteria>

<output>
Create .planning/phases/04-expansion-multi-tenant-verification/04-13-SUMMARY.md when done.
</output>

## Artifacts this phase produces

- backend/app/api/v1/forge_phase4/sessions.py::POST /responses, GET /responses/{id}, POST /responses/{id}/cancel, /input_items, GET /input_items
- backend/app/services/phase4_sessions.py::get_response, append_input_items
- backend/app/services/scheduler/responses_cleanup.py::daily_cleanup — 24h retention cron (NEW)
- env var: none
- new Alembic migration: none