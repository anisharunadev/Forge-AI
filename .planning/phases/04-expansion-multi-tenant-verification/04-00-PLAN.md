---
phase: "04"
plan_id: "04-00"
title: "Wave 0 — Alembic migration + Phase 4 test fixtures + 4 admin tab stubs"
wave: 0
depends_on: []
files_modified:
  - backend/alembic/versions/step_80_phase4_tables_001.py
  - backend/tests/phase4/__init__.py
  - backend/tests/phase4/conftest.py
  - backend/pytest.ini
  - apps/forge/components/admin/settings/CacheTab.tsx
  - apps/forge/components/admin/settings/FinOpsTab.tsx
  - apps/forge/components/admin/settings/VaultTab.tsx
  - apps/forge/components/admin/settings/RealtimeTab.tsx
  - apps/forge/components/admin/settings/index.ts
autonomous: true
requirements_addressed:
  - SCALE-F16-AC1
  - SCALE-F17-AC1
  - SCALE-F18-AC1
  - SCALE-F19-AC1
  - SCALE-F20-AC1
must_haves:
  truths:
    - "Every Phase 4 ORM model from backend/app/db/models/phase4.py has a corresponding Postgres table after `alembic upgrade head`."
    - "pytest backend/tests/phase4/ collects (not runs) every Phase 4 test file with no ImportError."
    - "An admin navigating to /admin opens the new Cache / FinOps / Vault / Realtime tabs without a 404 or runtime crash."
    - "Two real tenants can be created in tests via a fixture, satisfying R2 cross-tenant test discipline."
    - "The Phase 4 package `app.api.v1.forge_phase4` imports cleanly with no NotImplementedError raised at module load."
  prohibitions:
    - "MUST NOT edit any pre-existing alembic migration (append-only per backend/CLAUDE.md)."
    - - "MUST NOT add new Python packages — use already-pinned deps only."
    - - "MUST NOT delete or rewrite existing Phase 4 service modules (cache, providers, sessions, identity, ops); only verify and extend."
    - - "MUST NOT introduce any tenant_id=None default in fixture helpers (Rule 2)."
    - - "MUST NOT reference `settings.litellm_base_url` (does not exist; canonical is `settings.litellm_proxy_url`)."
---

<objective>
Land the Wave 0 foundation that every later Phase 4 plan depends on: a single alembic migration creating all 13 Phase 4 tables, a multi-tenant pytest conftest with a LiteLLM mock transport, and four missing admin tab stubs so the admin UI navigates without 404. This plan has no forward dependency; every other plan in this phase depends on it.
</objective>

<context>
@.planning/phases/04-expansion-multi-tenant-verification/00-source-spec.md
@.planning/phases/04-expansion-multi-tenant-verification/01-CONTEXT.md
@.planning/phases/04-expansion-multi-tenant-verification/04-RESEARCH.md

# Already merged skeleton (must NOT be redefined)
@backend/app/db/models/phase4.py
@backend/app/services/phase4_cache.py
@backend/app/services/phase4_providers.py
@backend/app/services/phase4_sessions.py
@backend/app/services/phase4_identity.py
@backend/app/services/phase4_ops.py
@backend/app/api/v1/forge_phase4/__init__.py
@backend/app/core/phase4_audit_events.py
@backend/app/core/phase4_errors.py

# Reference patterns
@backend/alembic/versions/step_78_f12_rbac_hierarchy.py
@backend/app/db/models/tenant.py
@backend/tests/conftest.py
@apps/forge/components/admin/settings/SSOTab.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create alembic migration for 13 Phase 4 tables with composite indexes and RLS GUC wiring</name>
  <files>backend/alembic/versions/step_80_phase4_tables_001.py</files>
  <read_first>
    - backend/app/db/models/phase4.py (full file — 13 ORM classes)
    - backend/alembic/versions/step_78_f12_rbac_hierarchy.py (migration pattern + TenantScopedMixin convention)
    - backend/app/db/models/tenant.py (TenantScopedMixin base class)
    - backend/CLAUDE.md (append-only migration rule, composite index pattern)
  </read_first>
  <acceptance_criteria>
    - `alembic upgrade head` succeeds against an empty Postgres 17 + Apache AGE + pgvector DB.
    - All 13 tables exist after upgrade: phase4_cache_keys, phase4_sessions, phase4_session_events, phase4_realtime_client_secrets, phase4_a2a_delegations, phase4_sso_configs, phase4_scim_tokens, phase4_oauth_clients, phase4_jwt_signing_keys, phase4_credentials, phase4_vault_configs, phase4_finops_exports, phase4_finops_settings.
    - Every table has `tenant_id UUID NOT NULL`, `project_id UUID NOT NULL`, and a composite index on `(tenant_id, project_id, ...)`.
    - `alembic downgrade -1` removes the 13 tables cleanly without touching prior migrations.
    - Phase4CacheKey has a `UNIQUE (tenant_id, project_id, key_hash)` constraint.
    - Phase4Session has a `CHECK (status IN ('active','disconnected','cancelled','expired','completed'))` constraint.
    - `python -c "from app.db.models.phase4 import Phase4CacheKey, Phase4Session, Phase4SessionEvent, Phase4RealtimeClientSecret, Phase4A2ADelegation, Phase4SsoConfig, Phase4ScimToken, Phase4OAuthClient, Phase4JwtSigningKey, Phase4Credential, Phase4VaultConfig, Phase4FinopsExport, Phase4FinopsSettings; print('ok')"` exits 0.
  </acceptance_criteria>
  <action>
    Author a new alembic revision file at `backend/alembic/versions/step_80_phase4_tables_001.py` with revision id auto-generated and `down_revision = 'step_78_f12_rbac_hierarchy'` (read the actual rev id from `step_78_f12_rbac_hierarchy.py` and pin it). Use `op.create_table()` for each of the 13 tables, mirroring the column types from `backend/app/db/models/phase4.py`. For every table include `tenant_id`, `project_id` (both UUID NOT NULL) and a `op.create_index()` composite on `(tenant_id, project_id, ...)`. On Phase4CacheKey add `UniqueConstraint('tenant_id', 'project_id', 'key_hash', name='uq_phase4_cache_key_hash')`. On Phase4Session add `CheckConstraint("status IN ('active','disconnected','cancelled','expired','completed')", name='ck_phase4_session_status')`. On Phase4JwtSigningKey add `CheckConstraint("status IN ('active','retired','revoked')", name='ck_phase4_jwt_key_status')`. Phase4Session.id is UUID v7 — set `server_default=sa.text(\"gen_random_uuid()\")` if not specified in the model. RLS GUC wiring is NOT in the migration (Rule 2 enforcement is at app layer via the existing `tenant_id` filter; do not duplicate in SQL).
  </action>
  <verify>
    ```bash
    cd /home/arunachalam.v@knackforge.com/forge-ai/backend
    alembic upgrade head 2>&1 | tail -5
    alembic downgrade -1 2>&1 | tail -3
    alembic upgrade head 2>&1 | tail -3
    psql "${DATABASE_URL}" -c "\\dt phase4_*" 2>&1 | grep -c phase4_
    # expect: 13
    ```
  </verify>
  <done>13 Phase 4 tables exist; upgrade/downgrade/upgrade cycle is clean; composite indexes present.</done>
</task>

<task type="auto">
  <name>Task 2: Create Phase 4 test package with multi-tenant conftest and LiteLLM mock transport</name>
  <files>backend/tests/phase4/__init__.py, backend/tests/phase4/conftest.py, backend/pytest.ini</files>
  <read_first>
    - backend/tests/conftest.py (existing fixtures pattern)
    - backend/app/db/models/tenant.py (Tenant model fields)
    - backend/app/db/session.py (get_session_factory)
    - backend/app/integrations/litellm/litellm_base_client.py (LiteLLMBaseClient structure to mock)
  </read_first>
  <acceptance_criteria>
    - `pytest backend/tests/phase4/ --collect-only` exits 0 with no ImportError.
    - `pytest backend/tests/phase4/__init__.py` succeeds (empty test set).
    - `from backend.tests.phase4.conftest import tenant_a, tenant_b, litellm_mock_transport` works in a one-liner.
    - `tenant_a` and `tenant_b` are pytest fixtures returning distinct (tenant_id, project_id, headers) tuples — `tenant_a.id != tenant_b.id`.
    - `litellm_mock_transport` is an `httpx.MockTransport` callable returning canned JSON for `/cache/ping`, `/cache/settings`, `/spend/logs`, `/routes`.
    - `backend/pytest.ini` contains `testpaths = tests` so the new `tests/phase4/` directory is auto-discovered (verify with `pytest --collect-only backend/tests/phase4`).
    - No fixture helper accepts `tenant_id=None` — passing None raises TypeError.
  </acceptance_criteria>
  <action>
    Create `backend/tests/phase4/__init__.py` as an empty marker. Create `backend/tests/phase4/conftest.py` defining: (a) `tenant_a` and `tenant_b` async fixtures that INSERT two rows into the `tenants` table with separate UUIDs and yield `{tenant_id: UUID, project_id: UUID, headers: dict}`; both rows also get `tenant_settings` JSONB entries with `forge.pass_through.openai.enabled = True` for tenant A and `False` for tenant B (configurable). (b) `litellm_mock_transport` fixture returning an `httpx.MockTransport` that handles the four LiteLLM paths above with deterministic JSON bodies. (c) `make_authenticated_client` factory fixture that builds an `httpx.AsyncClient` with the `X-Forge-Tenant` header injected. (d) Override `get_session_factory` via `app.dependency_overrides` so the test DB session is used. Edit `backend/pytest.ini` to add `testpaths = tests` if not present; do NOT remove existing asyncio_mode. Add a docstring to the file naming F16-F20 and citing the CONTEXT.md L2 multi-tenancy rule.
  </action>
  <verify>
    ```bash
    cd /home/arunachalam.v@knackforge.com/forge-ai/backend
    pytest tests/phase4/ --collect-only -q 2>&1 | tail -5
    # expect: no errors, "no tests ran" is acceptable here
    pytest tests/phase4/__init__.py -q 2>&1 | tail -3
    # expect: 1 passed (the empty __init__)
    grep -c testpaths pytest.ini
    # expect: 1
    ```
  </verify>
  <done>pytest collects phase4/ tests cleanly; multi-tenant fixtures available; LiteLLM mock transport returns canned responses.</done>
</task>

<task type="auto">
  <name>Task 3: Stub 4 missing admin tabs (Cache / FinOps / Vault / Realtime)</name>
  <files>apps/forge/components/admin/settings/CacheTab.tsx, apps/forge/components/admin/settings/FinOpsTab.tsx, apps/forge/components/admin/settings/VaultTab.tsx, apps/forge/components/admin/settings/RealtimeTab.tsx, apps/forge/components/admin/settings/index.ts</files>
  <read_first>
    - apps/forge/components/admin/settings/SSOTab.tsx (canonical stub pattern)
    - apps/forge/components/admin/settings/ProvidersTab.tsx (newer pattern)
    - apps/forge/components/admin/settings/SectionShell.tsx (shared shell wrapper)
    - apps/forge/components/admin/settings/index.ts (barrel export)
  </read_first>
  <acceptance_criteria>
    - All four files export a default React component named after the file (`CacheTab`, `FinOpsTab`, `VaultTab`, `RealtimeTab`).
    - Each stub renders inside `<SectionShell>` with a heading (e.g. "Cache settings — wired in plan 04-01") and a `<p>` body containing the placeholder copy.
    - `apps/forge/components/admin/settings/index.ts` exports all four new tabs.
    - `pnpm --filter forge-dashboard typecheck` exits 0 (no TS errors).
    - `grep -n "TODO" apps/forge/components/admin/settings/CacheTab.tsx` shows one comment referencing the next plan (04-01).
  </acceptance_criteria>
  <action>
    Create four stub components modeled exactly on `SSOTab.tsx`: each is a default-export function component rendering `<SectionShell title="<Tab Title>" subtitle="Phase 4 stub — wired in plan 04-NN"><div className="text-fg-muted"><p>This tab is a Phase 4 placeholder.</p></div></SectionShell>`. CacheTab title is "Cache"; FinOpsTab title is "FinOps"; VaultTab title is "Vault credentials"; RealtimeTab title is "Realtime sessions". Each file has a single `// TODO(step-80): wired in plan 04-NN` comment. Append the four new exports to `apps/forge/components/admin/settings/index.ts` in alphabetical order with the existing exports.
  </action>
  <verify>
    ```bash
    cd /home/arunachalam.v@knackforge.com/forge-ai/apps/forge
    ls components/admin/settings/{Cache,FinOps,Vault,Realtime}Tab.tsx
    grep -E "export (default )?(Cache|FinOps|Vault|Realtime)Tab" components/admin/settings/{Cache,FinOps,Vault,Realtime}Tab.tsx | wc -l
    # expect: 4
    pnpm --filter forge-dashboard typecheck 2>&1 | tail -5
    # expect: no errors
    ```
  </verify>
  <done>4 admin tab stubs render; sidebar navigation reaches them; typecheck clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration script → live DB | Auto-generated migrations can drop columns or skip renames; reviewer MUST diff before commit per backend/CLAUDE.md |
| conftest fixtures → real DB | Tests touch the same Postgres the dev server uses if TEST_DATABASE_URL is unset; must default to test DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-00-01 | Tampering | alembic autogenerate | medium | mitigate | Executor reviews diff before commit; downgrade/upgrade cycle verified |
| T-04-00-02 | Information Disclosure | conftest leak | low | accept | Tests use TEST_DATABASE_URL; if unset, skip with explicit error |
| T-04-00-03 | Denial of Service | admin tabs | low | accept | Stubs render empty; no backend calls |
| T-04-00-SC | Tampering | pip/npm installs | low | mitigate | No new packages added in this plan |

</threat_model>

<verification>
- `cd backend && alembic upgrade head && alembic downgrade -1 && alembic upgrade head` exits 0.
- `cd backend && pytest tests/phase4/ --collect-only -q` reports zero errors.
- `cd apps/forge && pnpm --filter forge-dashboard typecheck` exits 0.
- `grep -c "phase4_" backend/alembic/versions/step_80_phase4_tables_001.py` >= 13.
</verification>

<success_criteria>
- All three tasks committed atomically (one commit per task).
- Wave 0 plan summary written to `.planning/phases/04-expansion-multi-tenant-verification/04-00-SUMMARY.md`.
- 13 Phase 4 tables exist in Postgres.
- 4 admin tabs reachable from `/admin` with no runtime crash.
</success_criteria>

<output>
Create `.planning/phases/04-expansion-multi-tenant-verification/04-00-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

- `backend/alembic/versions/step_80_phase4_tables_001.py::op.create_table` × 13 — `Phase 4 tables: cache_keys, sessions, session_events, realtime_client_secrets, a2a_delegations, sso_configs, scim_tokens, oauth_clients, jwt_signing_keys, credentials, vault_configs, finops_exports, finops_settings`
- `backend/tests/phase4/__init__.py` — empty marker
- `backend/tests/phase4/conftest.py::tenant_a`, `tenant_b`, `litellm_mock_transport`, `make_authenticated_client` — multi-tenant test fixtures
- `apps/forge/components/admin/settings/CacheTab.tsx::CacheTab` — admin tab stub (wired in 04-01)
- `apps/forge/components/admin/settings/FinOpsTab.tsx::FinOpsTab` — admin tab stub (wired in 04-08)
- `apps/forge/components/admin/settings/VaultTab.tsx::VaultTab` — admin tab stub (wired in 04-07)
- `apps/forge/components/admin/settings/RealtimeTab.tsx::RealtimeTab` — admin tab stub (wired in 04-11)
- `apps/forge/components/admin/settings/index.ts` — barrel-export addition
- env var: none
- new Alembic migration: `backend/alembic/versions/step_80_phase4_tables_001.py`