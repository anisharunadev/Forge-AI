---
phase: "04"
plan_id: "04-10"
title: "F17 Realtime/A2A — SessionsService end-to-end (UUID v7 + DB state machine) + sessions routes + SessionsTab"
wave: 3
depends_on: ["04-00"]
files_modified:
  - backend/app/services/phase4_sessions.py
  - backend/app/api/v1/forge_phase4/sessions.py
  - backend/app/core/uuid7.py
  - apps/forge/components/admin/settings/SessionsTab.tsx
  - apps/forge/lib/api/sessions.ts
  - apps/forge/hooks/use-sessions.ts
  - backend/tests/phase4/test_sessions_state.py
autonomous: true
requirements_addressed:
  - SCALE-F17-AC1
  - SCALE-F17-AC4
  - SCALE-F17-AC5
  - SCALE-F17-AC8
  - SCALE-F17-AC10
must_haves:
  truths:
    - "create_session() returns a session_id that is a valid UUID v7 string (verified by uuid7.unpack() returning timestamp_ms)."
    - "POST /api/forge/sessions creates a Phase4Session row with status='active', expires_at = now() + MAX_DURATIONS[type], max_duration = MAX_DURATIONS[type]."
    - "POST /api/forge/sessions/{id}/extend doubles the max_duration and updates expires_at accordingly (verified by expires_at change)."
    - "resume_session(id) within 30s of last disconnect succeeds and sets status='active'; after 30s raises SessionResumeWindowExpired."
    - "cancel_session(id) sets status='cancelled' AND propagates the cancellation to all upstream LiteLLM calls within 100ms (verified by mocked httpx receiving a cancel signal)."
    - "Every session event (start, heartbeat, resume, cancel, expire) creates a Phase4SessionEvent row with session_id FK."
    - "SessionsTab UI lists active sessions with status badges and a Cancel button per row."
  prohibitions:
    - "MUST NOT use uuid.uuid4() (which is random, not time-ordered) — must use a UUID v7 implementation."
    - "MUST NOT store session state in module-level Python dict (Pitfall 3) — must use Phase4Session DB row."
    - "MUST NOT skip audit rows for cancel (FORGE_SESSIONS_CANCELLED audit constant required)."
    - "MUST NOT allow session state to survive a hard-delete — soft-delete via status='expired' or 'cancelled' only."
---

<objective>
Ship the F17 session lifecycle foundation: UUID v7 ids, DB-backed state machine with the six session types (realtime / a2a / background / eval / interaction / assistant / thread), create/extend/resume/cancel/expire operations, and the SessionsTab admin UI. The skeleton phase4_sessions.py exists but uses uuid.uuid4 — this plan replaces it with UUID v7 and wires the full state machine.
</objective>

<context>
@.planning/phases/04-expansion-multi-tenant-verification/00-source-spec.md (Feature 17 AC1, AC4, AC5, AC8, AC10)
@.planning/phases/04-expansion-multi-tenant-verification/01-CONTEXT.md (L5 DB-backed; UUID v7; 30s grace)
@.planning/phases/04-expansion-multi-tenant-verification/04-RESEARCH.md (Session State Architecture, Pitfall 3)

@backend/app/services/phase4_sessions.py (skeleton uses uuid.uuid4 — replace)
@backend/app/db/models/phase4.py::Phase4Session, Phase4SessionEvent
@backend/app/api/v1/forge_phase4/sessions.py (existing skeleton router)
@backend/app/core/phase4_audit_events.py (SESSIONS_STARTED, HEARTBEAT, RESUMED, CANCELLED, EXPIRED)
@apps/forge/components/admin/settings/SessionsTab.tsx (existing)
</context>

<tasks>

<task type="auto">
  <name>Task 1: UUID v7 helper + SessionsService state machine + router + SessionsTab wiring</name>
  <files>backend/app/core/uuid7.py, backend/app/services/phase4_sessions.py, backend/app/api/v1/forge_phase4/sessions.py, apps/forge/components/admin/settings/SessionsTab.tsx, apps/forge/lib/api/sessions.ts, apps/forge/hooks/use-sessions.ts, backend/tests/phase4/test_sessions_state.py</files>
  <read_first>
    - backend/app/services/phase4_sessions.py (existing methods; uses uuid.uuid4 — replace)
    - backend/app/db/models/phase4.py::Phase4Session, Phase4SessionEvent
    - backend/app/api/v1/forge_phase4/sessions.py (existing stub router)
    - backend/app/core/phase4_audit_events.py (existing SESSIONS_* constants)
    - apps/forge/components/admin/settings/SessionsTab.tsx (existing)
  </read_first>
  <acceptance_criteria>
    - backend/app/core/uuid7.py exposes uuid7() returning a UUID with version=7 (verified by uuid7.unpack() returning a timestamp_ms).
    - phase4_sessions.create_session() uses uuid7.uuid7() (NOT uuid.uuid4()).
    - POST /api/forge/sessions with body {session_type: 'realtime', agent_id: 'foo'} returns 200 + {session_id: <uuid v7>, status: 'active', expires_at: <iso>, max_duration: 14400}.
    - POST /api/forge/sessions/{id}/extend returns 200 + {new_max_duration, new_expires_at}; new_max_duration = 2 * current max_duration.
    - POST /api/forge/sessions/{id}/cancel returns 200 within 100ms; Phase4Session.status='cancelled'; forge.sessions.cancelled audit row.
    - Phase4SessionEvent rows exist for each operation (start, heartbeat, resume, cancel, expire).
    - SessionsTab shows table of active sessions (status badge + session_id + created_at) with per-row Cancel button.
    - pnpm --filter forge-dashboard typecheck exits 0.
  </acceptance_criteria>
  <action>
    Create backend/app/core/uuid7.py with functions: uuid7() -> UUID (uses time-based version 7 generation — see uuid7 spec at https://datatracker.ietf.org/doc/rfc9562/; implementation: 48-bit unix_ts_ms + 4-bit version=7 + 12-bit rand_a + 62-bit rand_b); unpack(uuid7) -> (timestamp_ms, rand_a, rand_b, variant). In backend/app/services/phase4_sessions.py, replace uuid.uuid4() with uuid7.uuid7() in create_session. Add methods: heartbeat(id, tenant_id, project_id) — UPDATE Phase4Session.last_heartbeat_at = now(); INSERT Phase4SessionEvent with type='heartbeat'. resume(id, tenant_id, project_id) — SELECT row FOR UPDATE; if status='disconnected' AND now() - last_heartbeat_at < RESUME_GRACE_SECONDS → status='active'; else raise SessionResumeWindowExpired. cancel(id, tenant_id, project_id) — UPDATE status='cancelled'; INSERT Phase4SessionEvent with type='cancelled'; audit; signal cancellation to all in-flight LiteLLM calls via a Redis pub/sub channel forge:phase4:cancel:<session_id> (publish 'cancel' message; subscribers receive within 100ms). In backend/app/api/v1/forge_phase4/sessions.py, add POST /sessions (create), POST /sessions/{id}/extend, POST /sessions/{id}/heartbeat, POST /sessions/{id}/resume, POST /sessions/{id}/cancel. In apps/forge/components/admin/settings/SessionsTab.tsx, replace existing stub with full implementation: list sessions (useSessions query), per-row Cancel button (useCancelSession mutation), status badges (active=green, disconnected=yellow, cancelled=red, expired=gray).
  </action>
  <verify>
    ```bash
    cd /home/arunachalam.v@knackforge.com/forge-ai/backend
    pytest tests/phase4/test_sessions_state.py::test_uuid_v7_id -x 2>&1 | tail -5
    pytest tests/phase4/test_sessions_state.py::test_extend_doubles_duration -x 2>&1 | tail -5
    pytest tests/phase4/test_sessions_state.py::test_cancel_within_100ms -x 2>&1 | tail -5
    pytest tests/phase4/test_sessions_state.py::test_resume_within_grace -x 2>&1 | tail -5
    cd /home/arunachalam.v@knackforge.com/forge-ai/apps/forge
    pnpm --filter forge-dashboard typecheck 2>&1 | tail -5
    ```
  </verify>
  <done>UUID v7 ids; full state machine; cancel propagates within 100ms; SessionsTab wired.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WebSocket client to sessions endpoint | JWT auth via api.ws() pattern; query ?token=<jwt> |
| Multi-replica state | Session rows are DB-backed; Redis pub/sub propagates cancel |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-10-01 | Spoofing | session resume race condition | medium | mitigate | resume_session uses SELECT ... FOR UPDATE row lock |
| T-04-10-02 | Denial of Service | session id collision (uuid4 was random) | low | accept | UUID v7 is time-ordered; collision probability negligible |
| T-04-10-03 | Repudiation | cancel not propagated | high | mitigate | Redis pub/sub forge:phase4:cancel:<id>; subscribers handle within 100ms |

</threat_model>

<verification>
- pytest backend/tests/phase4/test_sessions_state.py exits 0
- pnpm --filter forge-dashboard typecheck exits 0
- python -c "from app.core.uuid7 import uuid7; u = uuid7(); assert u.version == 7; print(u)"
</verification>

<success_criteria>
- One task committed.
- 04-10-SUMMARY.md written.
- F17 AC1, AC4, AC5, AC8, AC10 all have at least one passing test.
</success_criteria>

<output>
Create .planning/phases/04-expansion-multi-tenant-verification/04-10-SUMMARY.md when done.
</output>

## Artifacts this phase produces

- backend/app/core/uuid7.py::uuid7, unpack — UUID v7 implementation (NEW)
- backend/app/services/phase4_sessions.py::create_session — uses uuid7
- backend/app/services/phase4_sessions.py::heartbeat, resume, cancel, extend — full state machine
- backend/app/api/v1/forge_phase4/sessions.py::POST /sessions, /sessions/{id}/extend, /heartbeat, /resume, /cancel — 5 routes
- apps/forge/components/admin/settings/SessionsTab.tsx::SessionsTab — full implementation replacing stub
- apps/forge/lib/api/sessions.ts — typed API wrappers
- apps/forge/hooks/use-sessions.ts — TanStack Query hooks
- env var: REDIS_URL (existing — used for cancel pub/sub)
- new Alembic migration: none (Phase4Session + Phase4SessionEvent from 04-00)