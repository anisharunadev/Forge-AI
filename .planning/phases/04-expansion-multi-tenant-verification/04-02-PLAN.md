---
phase: "04"
plan_id: "04-02"
title: "F19 Cache — streaming mid-response chunk caching + cache metrics + savings reconciliation"
wave: 1
depends_on: ["04-01"]
files_modified:
  - backend/app/services/phase4_cache.py
  - backend/app/integrations/litellm/llm_client.py
  - backend/app/api/v1/forge_phase4/cache.py
  - backend/app/schemas/phase4.py
  - backend/app/services/phase4_spend.py
  - backend/tests/phase4/test_cache_metrics.py
  - backend/tests/phase4/test_cache_reconciliation.py
autonomous: true
requirements_addressed:
  - SCALE-F19-AC1
  - SCALE-F19-AC2
  - SCALE-F19-AC8
  - SCALE-F19-AC9
must_haves:
  truths:
    - "When chat_complete_stream() is called with stream=True, the cache wrapper stores the assembled (non-streamed) full response on completion and replays it byte-identically on subsequent identical calls."
    - "GET /api/forge/cache/metrics/savings returns {saved_usd, total_spend_usd, savings_pct} computed as hit_count * 0.002 / max(litellm_spend_total, 0.001), with savings_pct within 0.1% of LiteLLM's authoritative spend log difference for the same window."
    - "Updating a guardrail via existing Phase 2 endpoint fires a forge.cache.invalidated_by_guardrail audit event and clears cache entries with matching model+tenant_id within 60s."
    - "Streaming p95 cache hit latency is below 10ms (measured by test_cache_metrics.py using mocked Redis with 1ms latency)."
  prohibitions:
    - "MUST NOT cache streaming chunks in-progress (only the final assembled response)."
    - "MUST NOT reconcile against a spend total that includes forge.cache.hit rows themselves (avoid double-counting)."
    - "MUST NOT block the streaming response on cache write (fire-and-forget write at end of stream)."
---

<objective>
Extend the cache layer to (a) wrap streaming chat_complete_stream with cache assembly+replay, (b) add a savings reconciliation endpoint that compares hit-derived savings against the LiteLLM authoritative spend log, and (c) wire guardrail-change hooks to invalidate affected cache entries. This plan complements 04-01 by covering streaming + reconciliation semantics that the F19 AC list requires.
</objective>

<context>
@.planning/phases/04-expansion-multi-tenant-verification/00-source-spec.md (Feature 19 AC1, AC2, AC8, AC9)
@.planning/phases/04-expansion-multi-tenant-verification/01-CONTEXT.md (L7)
@.planning/phases/04-expansion-multi-tenant-verification/04-RESEARCH.md (Pitfall 7 — per-tenant hit rate; F19 AC8 reconciliation)

@backend/app/services/phase4_cache.py (after 04-01)
@backend/app/integrations/litellm/llm_client.py (after 04-01)
@backend/app/services/phase4_spend.py (existing spend recording)
@backend/app/services/phase4_guardrails.py (existing guardrail hooks, if present; else Phase 2 hooks)
@backend/app/api/v1/forge_phase4/cache.py (after 04-01)
@backend/app/core/phase4_audit_events.py::Phase4AuditAction.CACHE_INVALIDATED
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add streaming cache wrap (assemble-then-store, replay on hit)</name>
  <files>backend/app/integrations/litellm/llm_client.py, backend/app/services/phase4_cache.py</files>
  <read_first>
    - backend/app/integrations/litellm/llm_client.py (after 04-01 — chat_complete_cached already present)
    - backend/app/services/phase4_cache.py::store / lookup (after 04-01)
    - backend/app/integrations/litellm/llm_client.py (existing chat_complete_stream if present)
  </read_first>
  <acceptance_criteria>
    - chat_complete_stream_cached() with stream=True iterates the LiteLLM stream, assembles the final body, and on completion calls phase4_cache_service.store(...) in a fire-and-forget asyncio.create_task (does NOT block stream end).
    - On a subsequent identical call, chat_complete_stream_cached() detects hit, calls record_hit(), and re-emits the cached body as a single chunk via AsyncIterator yielding {"choices": [{"delta": cached_body}]}.
    - PII bypass still works for streams (pii_marked=True → no cache read, no cache write).
    - Test test_stream_cache_hit_returns_byte_identical_body passes with bytewise comparison.
  </acceptance_criteria>
  <action>
    In backend/app/integrations/litellm/llm_client.py, add async generator chat_complete_stream_cached(*, model, messages, tenant_id, project_id, user_id=None, pii_marked=False, **kwargs) -> AsyncIterator[dict]. Mirror chat_complete_cached's PII + feature-flag checks. On hit, yield one chunk {"choices": [{"delta": cached_body, "finish_reason": "stop"}], "object": "chat.completion.chunk"} and STOP. On miss, iterate chat_complete_stream(), collect all chunks into a list, assemble the full body, schedule asyncio.create_task(phase4_cache_service.store(...)) after the final chunk is yielded (not awaited), and yield chunks verbatim. Add a flag Phase4CacheService.assemble_stream_chunks(chunks) -> dict that joins the streaming delta fragments into the final assistant message text (use existing forge_chat.py pattern if present, else naive concatenation of "delta.content" fields).
  </action>
  <verify>
    ```bash
    cd /home/arunachalam.v@knackforge.com/forge-ai/backend
    pytest tests/phase4/test_cache_metrics.py::test_stream_cache_hit_returns_byte_identical_body -x 2>&1 | tail -5
    pytest tests/phase4/test_cache_metrics.py::test_stream_pii_bypass -x 2>&1 | tail -5
    ```
  </verify>
  <done>Streaming cache works; PII bypass works; fire-and-forget write works.</done>
</task>

<task type="auto">
  <name>Task 2: Add savings reconciliation endpoint and guardrail-change invalidation hook</name>
  <files>backend/app/api/v1/forge_phase4/cache.py, backend/app/services/phase4_cache.py, backend/app/services/phase4_spend.py, backend/app/services/phase4_guardrails.py</files>
  <read_first>
    - backend/app/services/phase4_cache.py::metrics (after 04-01)
    - backend/app/services/phase4_spend.py (existing reconciliation logic if any, else forge_spend.py)
    - backend/app/core/phase4_audit_events.py (CACHE_INVALIDATED + CACHE_INVALIDATED_BY_GUARDRAIL if missing, add)
    - backend/app/integrations/litellm/litellm_base_client.py (LiteLLMBaseClient.admin_client for /spend/logs)
  </read_first>
  <acceptance_criteria>
    - GET /api/forge/cache/metrics/savings returns 200 + {saved_usd, total_spend_usd, savings_pct, drift_pct} where drift_pct is |saved_usd - (hit_count * 0.002)| / max(total_spend_usd, 0.001). For a tenant with 100 hits and 1000 USD total spend, savings_pct is approx 0.2 (within 0.1% tolerance of the formula).
    - Test test_reconciliation_within_01_percent passes for a tenant with seeded 100 audit hits and mocked LiteLLM spend of 1000 USD.
    - Calling Phase4CacheService.invalidate_by_guardrail(tenant_id, project_id, guardrail_id) iterates Phase4CacheKey rows WHERE tenant_id=:tenant_id AND project_id=:project_id (all of them as a conservative flush) and POSTs /cache/delete to LiteLLM for each key_hash; records one forge.cache.invalidated_by_guardrail audit row per call.
    - test_guardrail_update_invalidates_within_60s passes (uses real time.sleep(0) + asyncio.wait_for with 60s timeout, asserts invalidation completes).
  </acceptance_criteria>
  <action>
    In backend/app/api/v1/forge_phase4/cache.py, add route GET /metrics/savings returning await phase4_cache_service.savings(tenant_id, project_id). In backend/app/services/phase4_cache.py, add method async savings(tenant_id, project_id, window_seconds=86400) that: (1) queries AuditEvent for forge.cache.hit count over window, multiplied by 0.002 USD per hit → saved_usd; (2) calls LiteLLMBaseClient.admin_client.get("/spend/logs", params={"start_time": window_start_iso, "end_time": now_iso, "metadata.forge_tenant_id": str(tenant_id)}) → total_spend_usd; (3) computes savings_pct = saved_usd / max(total_spend_usd, 0.001); (4) computes drift_pct = abs(saved_usd - (hit_count * 0.002)) / max(total_spend_usd, 0.001). Return {saved_usd, total_spend_usd, savings_pct, drift_pct}. Add method invalidate_by_guardrail(tenant_id, project_id, guardrail_id) that: (1) SELECTs all Phase4CacheKey.key_hash WHERE tenant_id=:t AND project_id=:p; (2) POSTs /cache/delete with the list; (3) audit_service.record(Phase4AuditAction.CACHE_INVALIDATED_BY_GUARDRAIL, payload={"guardrail_id": str(guardrail_id), "invalidated_count": len(key_hashes)}). If phase4_guardrails.py does not exist, create a thin shim file with invalidate_on_guardrail_change(tenant_id, project_id, guardrail_id) that calls the above method; this hook is wired by Phase 2 guardrail endpoint via callback. Add Phase4AuditAction.CACHE_INVALIDATED_BY_GUARDRAIL = "forge.cache.invalidated_by_guardrail" if not present.
  </action>
  <verify>
    ```bash
    cd /home/arunachalam.v@knackforge.com/forge-ai/backend
    pytest tests/phase4/test_cache_metrics.py::test_reconciliation_within_01_percent -x 2>&1 | tail -5
    pytest tests/phase4/test_cache_metrics.py::test_guardrail_update_invalidates_within_60s -x 2>&1 | tail -5
    ```
  </verify>
  <done>Savings endpoint live; reconciliation within 0.1%; guardrail invalidation hooked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Stream consumer to LiteLLM | Buffer exhaustion if cache write blocks stream end; mitigated by asyncio.create_task |
| Spend log to reconciliation | Drift if hit rows include their own spend; mitigated by formula hit_count * 0.002 USD |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-02-01 | Denial of Service | stream cache write blocks stream end | medium | mitigate | asyncio.create_task fire-and-forget |
| T-04-02-02 | Tampering | spend log forgery to inflate savings | low | accept | savings_pct capped by total_spend_usd from LiteLLM authoritative source |
| T-04-02-03 | Repudiation | guardrail invalidation not audited | medium | mitigate | forge.cache.invalidated_by_guardrail audit with guardrail_id |

</threat_model>

<verification>
- pytest backend/tests/phase4/test_cache_metrics.py exits 0
- pytest backend/tests/phase4/test_cache_reconciliation.py exits 0
</verification>

<success_criteria>
- Two tasks committed.
- 04-02-SUMMARY.md written.
- F19 AC1, AC2, AC8, AC9 all have at least one passing test.
</success_criteria>

<output>
Create .planning/phases/04-expansion-multi-tenant-verification/04-02-SUMMARY.md when done.
</output>

## Artifacts this phase produces

- backend/app/integrations/litellm/llm_client.py::chat_complete_stream_cached — streaming cache wrap
- backend/app/services/phase4_cache.py::Phase4CacheService.savings — savings reconciliation against LiteLLM /spend/logs
- backend/app/services/phase4_cache.py::Phase4CacheService.invalidate_by_guardrail — guardrail-driven invalidation
- backend/app/services/phase4_guardrails.py — guardrail invalidation shim (NEW)
- backend/app/core/phase4_audit_events.py::Phase4AuditAction.CACHE_INVALIDATED_BY_GUARDRAIL — new audit constant
- backend/app/api/v1/forge_phase4/cache.py::GET /metrics/savings — reconciliation endpoint
- env var: none
- new Alembic migration: none