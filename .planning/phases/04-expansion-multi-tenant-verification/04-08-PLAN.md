---
phase: "04"
plan_id: "04-08"
title: "F20 FinOps — CloudZero export + reconciliation + FinOpsTab + spend-tagged cache invalidation"
wave: 2
depends_on: ["04-02", "04-07"]
files_modified:
  - backend/app/services/phase4_ops.py
  - backend/app/api/v1/forge_phase4/ops.py
  - backend/app/services/phase4_cache.py
  - backend/app/services/scheduler/finops_export.py
  - backend/app/schemas/phase4.py
  - apps/forge/components/admin/settings/FinOpsTab.tsx
  - apps/forge/lib/api/finops.ts
  - apps/forge/hooks/use-finops.ts
  - backend/tests/phase4/test_finops_reconciliation.py
autonomous: true
requirements_addressed:
  - SCALE-F20-AC3
  - SCALE-F20-AC4
  - SCALE-F20-AC8
must_haves:
  truths:
    - "POST /api/forge/finops/cloudzero/dry_run produces a valid CloudZero CUR payload without sending; payload includes line_item rows for the tenant's spend records within window."
    - "POST /api/forge/finops/cloudzero/export sends the payload to the configured CloudZero endpoint within 60s and returns {record_count, payload_sha256}."
    - "CloudZero export reconciles to within 0.5% of the authoritative LiteLLM spend log (LITELLM_SPEND_LOG_RECONCILE_TOLERANCE = 0.005); drift > 0.5% triggers forge.finops.drift_detected audit row + auto-retry (max 3) with exponential backoff."
    - "Nightly cron at 02:00 UTC runs the export under a Postgres advisory lock so multi-replica deploys do not double-export."
    - "Cost discount + margin config affects the cost reported in spend records (PATCH /api/forge/settings/cost_config with discount_pct/margin_pct → next chat_complete records cost with the adjustment)."
  prohibitions:
    - "MUST NOT run nightly export from multiple replicas simultaneously (Postgres advisory lock required)."
    - "MUST NOT include Forge-only fields (forge_run_id) in CloudZero payload (only standard CUR fields)."
    - "MUST NOT skip the reconciliation check after every export — drift > 0.5% blocks the export from being marked complete."
---

<objective>
Ship the F20 FinOps CloudZero export + reconciliation + nightly cron, with spend-tagged cache invalidation. The skeleton phase4_ops.py has hooks but no CloudZero/Vantage code; this plan adds the export service, dry-run + export + reconciliation endpoints, the advisory-locked cron, and the FinOpsTab UI.
</objective>

<context>
@.planning/phases/04-expansion-multi-tenant-verification/00-source-spec.md (Feature 20 AC3, AC4, AC8)
@.planning/phases/04-expansion-multi-tenant-verification/01-CONTEXT.md (L8 reconciliation within 0.5%)
@.planning/phases/04-expansion-multi-tenant-verification/04-RESEARCH.md (FinOps Architecture; Pitfall 6 — multi-replica double-export)

@backend/app/services/phase4_ops.py (existing skeleton)
@backend/app/api/v1/forge_phase4/ops.py (existing stub)
@backend/app/db/models/phase4.py::Phase4FinopsExport, Phase4FinopsSettings
@backend/app/services/scheduler/ (existing cron pattern, Postgres advisory lock)
@backend/app/integrations/litellm/litellm_base_client.py (admin_client for /spend/logs)
@backend/app/services/phase4_cache.py (spend-tagged invalidation hook)
</context>

<tasks>

<task type="auto">
  <name>Task 1: CloudZero export service + dry-run + export + reconciliation endpoints</name>
  <files>backend/app/services/phase4_ops.py, backend/app/api/v1/forge_phase4/ops.py, backend/app/schemas/phase4.py, backend/tests/phase4/test_finops_reconciliation.py</files>
  <read_first>
    - backend/app/services/phase4_ops.py (existing skeleton)
    - backend/app/integrations/litellm/litellm_base_client.py (LiteLLMBaseClient.admin_client)
    - backend/app/db/models/phase4.py::Phase4FinopsExport, Phase4FinopsSettings
    - docs/litellm/forge-litellm-integration.md §2.15 — FinOps endpoint mapping
  </read_first>
  <acceptance_criteria>
    - POST /api/forge/finops/cloudzero/dry_run with body {tenant_id, start, end} returns {payload: <CUR object>, record_count: N, payload_sha256: "..."} WITHOUT sending the payload.
    - POST /api/forge/finops/cloudzero/export with body {tenant_id, start, end} sends the payload to the configured endpoint, returns 200 + {record_count, payload_sha256, exported_at}.
    - After export, GET /api/forge/finops/cloudzero/reconciliation returns {total_local, total_litellm, drift_pct}. Drift_pct = |total_local - total_litellm| / max(total_litellm, 0.001). For seeded spend of 1000 USD with mocked LiteLLM returning 1005 USD, drift_pct = 0.005 (within 0.5% tolerance).
    - Test test_drift_over_threshold_audits passes: mock LiteLLM returns 1100 USD (drift 10%); assert forge.finops.drift_detected audit row + export marked as 'failed' (NOT 'completed').
    - All three endpoints require require_permission("forge.finops.admin").
  </acceptance_criteria>
  <action>
    In backend/app/services/phase4_ops.py, add class CloudZeroExporter with methods: (1) build_cur_payload(tenant_id, start, end) — queries SpendRecord rows WHERE tenant_id=:t AND created_at BETWEEN :start AND :end; transforms each row to CUR shape {lineItem/UsageAccountId: tenant_id, lineItem/UsageType: model, lineItem/UnblendedCost: cost_usd, lineItem/UsageStartDate: iso_timestamp}; returns {payload: {line_items: [...]}, record_count: N, payload_sha256: hashlib.sha256(json.dumps(payload).encode()).hexdigest()}. (2) dry_run(tenant_id, start, end) — calls build_cur_payload, returns without sending. (3) export(tenant_id, start, end, destination_url, api_key) — calls build_cur_payload, POSTs to destination_url with Authorization: Bearer api_key; on 2xx inserts Phase4FinopsExport row with status='completed'; on failure status='failed'. (4) reconcile(export_id) — pulls the LiteLLM /spend/logs with metadata.forge_tenant_id filter for the same window, computes drift_pct; if drift_pct > 0.005, INSERT audit row 'forge.finops.drift_detected' and UPDATE Phase4FinopsExport.status='failed'. (5) In backend/app/api/v1/forge_phase4/ops.py, add POST /finops/cloudzero/dry_run, POST /finops/cloudzero/export, GET /finops/cloudzero/reconciliation/{export_id} routes. In backend/app/schemas/phase4.py, add CloudZeroDryRunResponse, CloudZeroExportResponse, ReconciliationResponse Pydantic models. Add Phase4AuditAction.FINOPS_DRY_RUN_COMPLETED, FINOPS_EXPORTED, FINOPS_DRIFT_DETECTED if not present.
  </action>
  <verify>
    ```bash
    cd /home/arunachalam.v@knackforge.com/forge-ai/backend
    pytest tests/phase4/test_finops_reconciliation.py::test_dry_run_no_send -x 2>&1 | tail -5
    pytest tests/phase4/test_finops_reconciliation.py::test_export_within_60s -x 2>&1 | tail -5
    pytest tests/phase4/test_finops_reconciliation.py::test_drift_over_threshold_audits -x 2>&1 | tail -5
    pytest tests/phase4/test_finops_reconciliation.py::test_reconciliation_within_05_percent -x 2>&1 | tail -5
    ```
  </verify>
  <done>CloudZero dry-run + export + reconciliation work; drift detected and audit fires.</done>
</task>

<task type="auto">
  <name>Task 2: Postgres advisory lock nightly cron + FinOpsTab UI + spend-tagged cache invalidation</name>
  <files>backend/app/services/scheduler/finops_export.py, apps/forge/components/admin/settings/FinOpsTab.tsx, apps/forge/lib/api/finops.ts, apps/forge/hooks/use-finops.ts, backend/app/services/phase4_cache.py, backend/tests/phase4/test_finops_cron.py</files>
  <read_first>
    - backend/app/services/scheduler/ (existing cron pattern with Postgres advisory lock)
    - apps/forge/components/admin/settings/FinOpsTab.tsx (stub from 04-00)
    - backend/app/services/phase4_cache.py::invalidate_by_guardrail (after 04-02)
  </read_first>
  <acceptance_criteria>
    - Nightly cron registers with APScheduler at 02:00 UTC; uses SELECT pg_advisory_lock(hashtext('forge.finops.export')) before starting.
    - Test test_cron_uses_advisory_lock passes: two concurrent calls to the cron; second call returns immediately with status='skipped_lock'.
    - FinOpsTab shows: CloudZero config form (api_key, destination_url), Recent Exports table (last 10 rows from Phase4FinopsExport), Reconciliation status indicator.
    - PATCH /api/forge/settings/cost_config with {discount_pct: 10, margin_pct: 5} updates Tenant.settings['cost_config']; subsequent spend records include the adjustment.
    - PATCH /api/forge/cache/invalidate_by_cost_tag with {model: "gpt-4o"} calls phase4_cache_service.invalidate_by_cost_tag (which deletes Phase4CacheKey rows with matching model).
    - pnpm --filter forge-dashboard typecheck exits 0.
  </acceptance_criteria>
  <action>
    Create backend/app/services/scheduler/finops_export.py with async function nightly_export() that: (1) SELECT pg_advisory_lock(hashtext('forge.finops.export')); (2) iterates tenants with Phase4FinopsSettings.auto_export=True; (3) calls CloudZeroExporter.export for each; (4) releases lock. Register the function with APScheduler via app.main lifespan. In apps/forge/components/admin/settings/FinOpsTab.tsx, replace stub with full UI. Add backend/app/services/phase4_cache.py::invalidate_by_cost_tag(tenant_id, model) method that DELETEs Phase4CacheKey rows WHERE tenant_id=:t AND model=:model AND calls LiteLLM /cache/delete for each. Add POST /api/forge/cache/invalidate_by_cost_tag route. Add PATCH /api/forge/settings/cost_config route.
  </action>
  <verify>
    ```bash
    cd /home/arunachalam.v@knackforge.com/forge-ai/backend
    pytest tests/phase4/test_finops_cron.py::test_cron_uses_advisory_lock -x 2>&1 | tail -5
    pytest tests/phase4/test_finops_cron.py::test_cost_config_affects_spend -x 2>&1 | tail -5
    cd /home/arunachalam.v@knackforge.com/forge-ai/apps/forge
    pnpm --filter forge-dashboard typecheck 2>&1 | tail -5
    ```
  </verify>
  <done>Nightly cron advisory-locked; FinOpsTab UI live; cost config affects spend; cost-tagged invalidation works.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Forge Backend to CloudZero API | API key transmitted over TLS; payload signed with destination credentials |
| Multiple Forge replicas | Nightly cron must use Postgres advisory lock |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-08-01 | Tampering | CloudZero URL injection | high | mitigate | Validate destination_url matches regex ^https://(api\.)?cloudzero\.com/ |
| T-04-08-02 | Denial of Service | multi-replica double-export | high | mitigate | Postgres advisory lock before cron body |
| T-04-08-03 | Repudiation | drift not audited | medium | mitigate | forge.finops.drift_detected audit row + status='failed' |

</threat_model>

<verification>
- pytest backend/tests/phase4/test_finops_reconciliation.py exits 0
- pytest backend/tests/phase4/test_finops_cron.py exits 0
- pnpm --filter forge-dashboard typecheck exits 0
</verification>

<success_criteria>
- Two tasks committed.
- 04-08-SUMMARY.md written.
- F20 AC3, AC4, AC8 all have at least one passing test.
</success_criteria>

<output>
Create .planning/phases/04-expansion-multi-tenant-verification/04-08-SUMMARY.md when done.
</output>

## Artifacts this phase produces

- backend/app/services/phase4_ops.py::CloudZeroExporter — CUR payload builder + dry-run + export + reconcile
- backend/app/services/phase4_ops.py::Phase4OpsService.update_cost_config — cost discount/margin
- backend/app/services/scheduler/finops_export.py::nightly_export — advisory-locked cron (NEW)
- backend/app/services/phase4_cache.py::Phase4CacheService.invalidate_by_cost_tag — model-scoped invalidation
- backend/app/api/v1/forge_phase4/ops.py::POST /finops/cloudzero/dry_run, /export, GET /reconciliation/{id}
- backend/app/api/v1/forge_phase4/ops.py::PATCH /settings/cost_config
- backend/app/api/v1/forge_phase4/cache.py::POST /invalidate_by_cost_tag
- backend/app/schemas/phase4.py::CloudZeroDryRunResponse, CloudZeroExportResponse, ReconciliationResponse
- backend/app/core/phase4_audit_events.py::Phase4AuditAction.FINOPS_DRY_RUN_COMPLETED, FINOPS_EXPORTED, FINOPS_DRIFT_DETECTED
- apps/forge/components/admin/settings/FinOpsTab.tsx::FinOpsTab — full implementation
- apps/forge/lib/api/finops.ts — typed API wrappers
- apps/forge/hooks/use-finops.ts — TanStack Query hooks
- env var: CLOUDZERO_API_KEY (per-tenant via Phase4FinopsSettings), CLOUDZERO_DESTINATION_URL
- new Alembic migration: none