---
phase: "04"
plan_id: "04-03"
title: "F19 Cache — PII skip, cross-tenant isolation probe, tenant-offboard purge, admin invalidate UI"
wave: 1
depends_on: ["04-01", "04-02"]
files_modified:
  - backend/app/services/phase4_cache.py
  - backend/app/services/tenant_offboard.py
  - backend/app/api/v1/forge_phase4/cache.py
  - apps/forge/components/admin/settings/CacheTab.tsx
  - backend/tests/phase4/test_cache_isolation.py
  - backend/tests/phase4/test_cache_offboard.py
autonomous: true
requirements_addressed:
  - SCALE-F19-AC6
  - SCALE-F19-AC7
  - SCALE-F19-AC10
must_haves:
  truths:
    - "Cross-tenant probe test: tenant_b calling phase4_cache_service.lookup(tenant_a_cache_key_hash) returns None even if the underlying Redis key exists (defense in depth via tenant_id filter on Phase4CacheKey row)."
    - "Tenant A cache rows are NEVER returned in tenant B metrics aggregation (Phase4CacheKey.tenant_id column enforces DB-side isolation)."
    - "Calling Phase4CacheService.purge_tenant(tenant_id, project_id) deletes all Phase4CacheKey rows AND calls LiteLLM /cache/delete for every key_hash, recording one forge.cache.purged_tenant audit row."
    - "Tenant offboard workflow (services/tenant_offboard.py::offboard_tenant) calls purge_tenant() before soft-deleting the tenant."
    - "CacheTab admin UI shows a 'Flush all (double-confirm)' button that POSTs /api/forge/cache/flushall with confirm='FLUSHALL' only after typing the literal 'FLUSHALL' in a confirmation input."
  prohibitions:
    - "MUST NOT rely solely on Redis key prefix for cross-tenant isolation — DB filter on Phase4CacheKey.tenant_id is mandatory (Pitfall 7 defense in depth)."
    - "MUST NOT allow flushall without the literal 'FLUSHALL' string confirmation."
    - "MUST NOT skip the offboard purge even if Redis call fails — record forge.cache.offboard_purge_partial audit and continue."
---

<objective>
Close the F19 cross-tenant isolation and compliance gaps: add a probe-test that proves tenant_b cannot see tenant_a's cached data, add tenant-offboard purge that flushes all keys for a tenant before soft-delete, and ship the admin UI for flushall double-confirmation. This plan finalizes the F19 acceptance criteria set.
</objective>

<context>
@.planning/phases/04-expansion-multi-tenant-verification/00-source-spec.md (Feature 19 AC6, AC7, AC10)
@.planning/phases/04-expansion-multi-tenant-verification/01-CONTEXT.md (L7 compliance: tenant-offboard purges all keys)
@.planning/phases/04-expansion-multi-tenant-verification/04-RESEARCH.md (Pitfall 7 — per-tenant hit rate + cross-tenant defense)

@backend/app/services/phase4_cache.py (after 04-01, 04-02)
@backend/app/db/models/phase4.py::Phase4CacheKey
@backend/app/services/tenant_offboard.py (existing offboard workflow if any, else create)
@backend/app/api/v1/forge_phase4/cache.py (after 04-01, 04-02)
@apps/forge/components/admin/settings/CacheTab.tsx (after 04-01)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cross-tenant isolation probe test + tenant-offboard purge hook</name>
  <files>backend/app/services/phase4_cache.py, backend/app/services/tenant_offboard.py, backend/tests/phase4/test_cache_isolation.py, backend/tests/phase4/test_cache_offboard.py</files>
  <read_first>
    - backend/app/db/models/phase4.py::Phase4CacheKey (tenant_id + project_id columns + composite index)
    - backend/app/services/phase4_cache.py::lookup / store / metrics (after 04-01)
    - backend/app/services/tenant_offboard.py (existing offboard flow, if any; else grep for tenant.delete / soft_delete pattern)
    - backend/tests/phase4/conftest.py (tenant_a / tenant_b fixtures from 04-00)
  </read_first>
  <acceptance_criteria>
    - test_cross_tenant_probe_returns_none passes: with tenant_a having a stored cache row, tenant_b calling phase4_cache_service.lookup(key_hash, tenant_b.tenant_id) returns None even though the underlying Redis key still exists (verified by direct httpx call to /cache/get?key=<hash> returning the value).
    - test_metrics_aggregation_filters_by_tenant passes: 100 hits for tenant_a and 50 hits for tenant_b in the audit table; tenant_a sees hit_count=100, tenant_b sees hit_count=50.
    - test_offboard_purge_deletes_all_keys passes: after offboarding tenant_a, Phase4CacheKey has 0 rows WHERE tenant_id=tenant_a.id, AND LiteLLM /cache/delete was called with all key hashes.
    - Phase4CacheService.purge_tenant(tenant_id, project_id) records one forge.cache.purged_tenant audit row with payload {deleted_count: N}.
  </acceptance_criteria>
  <action>
    In backend/app/services/phase4_cache.py, modify lookup() so that BEFORE returning the LiteLLM body, it queries Phase4CacheKey WHERE tenant_id=:tenant_id AND project_id=:project_id AND key_hash=:key_hash; if no row matches, return None even if LiteLLM returns a value (defense in depth). Modify metrics() so the aggregate query joins Phase4CacheKey on (tenant_id, project_id, key_hash) AND Phase4CacheKey.tenant_id = :tenant_id (DB-side filter, not just audit action filter). Add method async purge_tenant(tenant_id, project_id) that: (1) SELECTs all Phase4CacheKey rows WHERE tenant_id=:t AND project_id=:p; (2) calls LiteLLMBaseClient.admin_client.post("/cache/delete", json={"keys": [r.key_hash for r in rows]}); (3) DELETE FROM phase4_cache_keys WHERE tenant_id=:t AND project_id=:p; (4) audit_service.record(Phase4AuditAction.CACHE_PURGED_TENANT, payload={"deleted_count": len(rows)}). Add Phase4AuditAction.CACHE_PURGED_TENANT = "forge.cache.purged_tenant" to phase4_audit_events.py. In backend/app/services/tenant_offboard.py (create if missing — file path: app/services/tenant_offboard.py), add async function offboard_tenant(tenant_id, project_id) that: (1) calls phase4_cache_service.purge_tenant(tenant_id, project_id); (2) calls existing tenant soft-delete logic (Phase 2 may already have this — grep first). Cross-tenant probe test uses tenant_a.store() then tenant_b.lookup(tenant_a.key_hash, tenant_b.tenant_id) — asserts None.
  </action>
  <verify>
    ```bash
    cd /home/arunachalam.v@knackforge.com/forge-ai/backend
    pytest tests/phase4/test_cache_isolation.py::test_cross_tenant_probe_returns_none -x 2>&1 | tail -5
    pytest tests/phase4/test_cache_isolation.py::test_metrics_aggregation_filters_by_tenant -x 2>&1 | tail -5
    pytest tests/phase4/test_cache_offboard.py::test_offboard_purge_deletes_all_keys -x 2>&1 | tail -5
    ```
  </verify>
  <done>Cross-tenant isolation enforced both at DB and Redis layers; offboard purge hooks into tenant deletion; audit recorded.</done>
</task>

<task type="auto">
  <name>Task 2: CacheTab admin flushall UI with double-confirmation</name>
  <files>apps/forge/components/admin/settings/CacheTab.tsx, backend/app/api/v1/forge_phase4/cache.py</files>
  <read_first>
    - apps/forge/components/admin/settings/CacheTab.tsx (after 04-01)
    - backend/app/api/v1/forge_phase4/cache.py::POST /flushall (after 04-01)
  </read_first>
  <acceptance_criteria>
    - CacheTab shows a DangerZone section with a 'Flush all cache' button (initially disabled).
    - Below the button is a text input labeled "Type FLUSHALL to confirm" — the button enables ONLY when the input value equals the literal string 'FLUSHALL'.
    - Clicking the enabled button shows a browser-native confirm() dialog with the text "This will delete ALL cache entries for this tenant. Continue?".
    - On confirm, POST /api/forge/cache/flushall with {confirm: 'FLUSHALL'} is sent; on success, a toast appears with the deleted count.
    - pnpm --filter forge-dashboard typecheck exits 0.
  </acceptance_criteria>
  <action>
    In apps/forge/components/admin/settings/CacheTab.tsx, add a new DangerZone section at the bottom (after Metrics). Use a useState<string>('') for the confirm input. Render a destructive button with `disabled={confirmInput !== 'FLUSHALL'}`. On click, call window.confirm("This will delete ALL cache entries for this tenant. Continue?") — if true, call useCacheFlushall().mutate({confirm: 'FLUSHALL'}). On success, invalidate useCacheMetrics() query. Add a Banner/Alert element (use existing design-system Banner) with text "This action is irreversible. All cache entries for this tenant will be deleted from LiteLLM." above the input. In backend/app/api/v1/forge_phase4/cache.py, ensure POST /flushall is already gated by require_permission("forge.cache.admin") and rejects any payload where confirm != 'FLUSHALL' with HTTPException(422).
  </action>
  <verify>
    ```bash
    cd /home/arunachalam.v@knackforge.com/forge-ai/apps/forge
    pnpm --filter forge-dashboard typecheck 2>&1 | tail -5
    grep -c "FLUSHALL" components/admin/settings/CacheTab.tsx
    # expect: at least 3 (label, button, post body)
    ```
  </verify>
  <done>Flushall UI enforces literal-string confirm; backend 422 on missing/wrong confirm.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tenant A to Tenant B cache | Cross-tenant data leak; mitigated by tenant_id column on Phase4CacheKey + Redis key prefix |
| Offboard workflow to live cache | Lingering tenant data; mitigated by purge_tenant before soft-delete |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-03-01 | Information Disclosure | cross-tenant probe | high | mitigate | DB filter on Phase4CacheKey.tenant_id; defense in depth |
| T-04-03-02 | Repudiation | offboard purge not audited | medium | mitigate | forge.cache.purged_tenant audit row |
| T-04-03-03 | Denial of Service | accidental flushall | high | mitigate | literal-string double-confirm |

</threat_model>

<verification>
- pytest backend/tests/phase4/test_cache_isolation.py exits 0
- pytest backend/tests/phase4/test_cache_offboard.py exits 0
- pnpm --filter forge-dashboard typecheck exits 0
</verification>

<success_criteria>
- Two tasks committed.
- 04-03-SUMMARY.md written.
- F19 AC6, AC7, AC10 all have at least one passing test.
</success_criteria>

<output>
Create .planning/phases/04-expansion-multi-tenant-verification/04-03-SUMMARY.md when done.
</output>

## Artifacts this phase produces

- backend/app/services/phase4_cache.py::Phase4CacheService.purge_tenant — tenant offboard cache purge
- backend/app/services/phase4_cache.py::Phase4CacheService.lookup — DB-filter added (defense in depth)
- backend/app/services/tenant_offboard.py::offboard_tenant — wraps purge_tenant before soft-delete (NEW file if missing)
- backend/app/core/phase4_audit_events.py::Phase4AuditAction.CACHE_PURGED_TENANT — new audit constant
- apps/forge/components/admin/settings/CacheTab.tsx::DangerZone — flushall double-confirm UI
- env var: none
- new Alembic migration: none