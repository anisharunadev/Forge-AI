---
phase: "04"
plan_id: "04-09"
title: "F20 FinOps — Vantage export + email event settings + EmailSettingsTab + cost config UI"
wave: 2
depends_on: ["04-08"]
files_modified:
  - backend/app/services/phase4_ops.py
  - backend/app/api/v1/forge_phase4/ops.py
  - backend/app/schemas/phase4.py
  - apps/forge/components/admin/settings/EmailSettingsTab.tsx
  - backend/tests/phase4/test_vantage_export.py
  - backend/tests/phase4/test_email_settings.py
autonomous: true
requirements_addressed:
  - SCALE-F20-AC5
  - SCALE-F20-AC7
must_haves:
  truths:
    - "Vantage export mirror CloudZero exactly: POST /api/forge/finops/vantage/dry_run + /export + GET /reconciliation/{id} work identically to CloudZero endpoints."
    - "Email event setting update changes which emails are sent within 5 minutes (event-driven, not polled): PATCH /api/forge/settings/email_events with {event_type: enabled} updates Tenant.settings['email_events']; the notification service checks this map before sending each email."
    - "EmailSettingsTab UI shows toggles for each event_type (cost_threshold_exceeded, budget_warning, weekly_digest, monthly_digest, sso_provision_failure)."
  prohibitions:
    - "MUST NOT poll the email_events setting on each send (event-driven check only — PATCH must invalidate the in-memory cache or use Tenant.settings JSONB directly)."
    - "MUST NOT hardcode the email event list in the UI — read from /api/forge/settings/email_events/schema which returns the canonical list from Phase 1 notification service if it exists; else from a constant in phase4_ops.py."
---

<objective>
Ship the Vantage export mirror + email event settings. The Vantage export reuses the CloudZero exporter (only the destination URL + payload shape differ slightly). Email event settings drive which transactional emails are sent.
</objective>

<context>
@.planning/phases/04-expansion-multi-tenant-verification/00-source-spec.md (Feature 20 AC5, AC7)
@.planning/phases/04-expansion-multi-tenant-verification/01-CONTEXT.md (L8)
@.planning/phases/04-expansion-multi-tenant-verification/04-RESEARCH.md (FinOps Architecture, Email Settings)

@backend/app/services/phase4_ops.py (after 04-08 — CloudZeroExporter)
@backend/app/api/v1/forge_phase4/ops.py (after 04-08)
@backend/app/services/notification_service.py (existing — verify event-driven pattern)
@backend/app/db/models/tenant.py::Tenant.settings JSONB
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vantage exporter + email event settings service + UI</name>
  <files>backend/app/services/phase4_ops.py, backend/app/api/v1/forge_phase4/ops.py, backend/app/schemas/phase4.py, apps/forge/components/admin/settings/EmailSettingsTab.tsx, backend/tests/phase4/test_vantage_export.py, backend/tests/phase4/test_email_settings.py</files>
  <read_first>
    - backend/app/services/phase4_ops.py::CloudZeroExporter (after 04-08)
    - backend/app/services/notification_service.py (existing send_email logic if any)
    - backend/app/db/models/tenant.py::Tenant.settings JSONB
  </read_first>
  <acceptance_criteria>
    - VantageExporter class mirrors CloudZeroExporter: build_vantage_payload, dry_run, export, reconcile methods.
    - POST /api/forge/finops/vantage/dry_run + /export + GET /reconciliation/{id} all return identical shape to CloudZero endpoints.
    - Test test_vantage_export_within_60s passes (mirrors test_export_within_60s).
    - PATCH /api/forge/settings/email_events with body {cost_threshold_exceeded: false} updates Tenant.settings['email_events'] and returns 200.
    - notification_service.send_email(event_type=...) checks Tenant.settings['email_events'][event_type] before sending; if False, returns without sending AND records forge.notifications.skipped audit row.
    - EmailSettingsTab renders toggles for 5 event types; toggling updates the setting via PATCH.
    - Test test_email_event_toggle_blocks_send passes: enable=False then trigger notification; assert no email sent + audit row.
  </acceptance_criteria>
  <action>
    In backend/app/services/phase4_ops.py, add class VantageExporter mirroring CloudZeroExporter (same shape, different destination_url pattern https://api.vantage.sh/v1/...). In backend/app/api/v1/forge_phase4/ops.py, add POST /finops/vantage/dry_run, POST /finops/vantage/export, GET /finops/vantage/reconciliation/{id}. Add EmailEventSettings schema with the 5 event types (cost_threshold_exceeded, budget_warning, weekly_digest, monthly_digest, sso_provision_failure). Add GET /settings/email_events, PATCH /settings/email_events routes. In backend/app/services/notification_service.py, add a check at the top of send_email(): SELECT Tenant.settings['email_events'][event_type]; if not truthy, return without sending and audit_service.record(Phase4AuditAction.NOTIFICATIONS_SKIPPED, payload={"event_type": event_type}). Create apps/forge/components/admin/settings/EmailSettingsTab.tsx (not in stub list — new file) with toggle for each event type.
  </action>
  <verify>
    ```bash
    cd /home/arunachalam.v@knackforge.com/forge-ai/backend
    pytest tests/phase4/test_vantage_export.py -x 2>&1 | tail -10
    pytest tests/phase4/test_email_settings.py::test_email_event_toggle_blocks_send -x 2>&1 | tail -5
    cd /home/arunachalam.v@knackforge.com/forge-ai/apps/forge
    pnpm --filter forge-dashboard typecheck 2>&1 | tail -5
    ```
  </verify>
  <done>Vantage export works; email event toggle blocks send within 5min; EmailSettingsTab live; typecheck clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Forge Backend to Vantage API | API key + destination URL must be per-tenant |
| Email sender to user | Per-tenant event settings must be respected before send |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-09-01 | Information Disclosure | Vantage URL injection | high | mitigate | Validate destination_url matches ^https://(api\.)?vantage\.sh/ |
| T-04-09-02 | Repudiation | email sent despite disabled setting | medium | mitigate | notification_service.send_email checks Tenant.settings['email_events'][event_type] |

</threat_model>

<verification>
- pytest backend/tests/phase4/test_vantage_export.py exits 0
- pytest backend/tests/phase4/test_email_settings.py exits 0
- pnpm --filter forge-dashboard typecheck exits 0
</verification>

<success_criteria>
- One task committed.
- 04-09-SUMMARY.md written.
- F20 AC5, AC7 both have at least one passing test.
</success_criteria>

<output>
Create .planning/phases/04-expansion-multi-tenant-verification/04-09-SUMMARY.md when done.
</output>

## Artifacts this phase produces

- backend/app/services/phase4_ops.py::VantageExporter — Vantage mirror of CloudZero
- backend/app/api/v1/forge_phase4/ops.py::POST /finops/vantage/{dry_run, export}, GET /finops/vantage/reconciliation/{id}
- backend/app/api/v1/forge_phase4/ops.py::GET /settings/email_events, PATCH /settings/email_events
- backend/app/services/notification_service.py::send_email — pre-send check for email_events setting
- backend/app/schemas/phase4.py::EmailEventSettings
- backend/app/core/phase4_audit_events.py::Phase4AuditAction.NOTIFICATIONS_SKIPPED
- apps/forge/components/admin/settings/EmailSettingsTab.tsx::EmailSettingsTab — new tab (not in stub list)
- env var: VANTAGE_API_KEY, VANTAGE_DESTINATION_URL
- new Alembic migration: none