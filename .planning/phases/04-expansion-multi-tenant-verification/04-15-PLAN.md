---
phase: "04"
plan_id: "04-15"
title: "F18 SCIM v2 Groups + ServiceProviderConfig + filter parser + OAuth server (authorize/token/register/fallback)"
wave: 4
depends_on: ["04-14"]
files_modified:
  - backend/app/services/phase4_identity.py
  - backend/app/api/v1/forge_phase4/identity.py
  - backend/app/main.py
  - backend/app/schemas/phase4.py
  - backend/app/core/phase4_audit_events.py
  - backend/app/services/scim_filter_parser.py
  - backend/tests/phase4/test_scim_groups.py
  - backend/tests/phase4/test_oauth_server.py
autonomous: true
requirements_addressed:
  - SCALE-F18-AC6
  - SCALE-F18-AC8
must_haves:
  truths:
    - "POST /scim/v2/Groups creates a Group row + assigns members (referenced by User id)."
    - "GET /scim/v2/ServiceProviderConfig returns {documentationUri, patch: {supported: true}, bulk: {supported: false}, filter: {supported: true, maxResults: 200}}."
    - "SCIM filter parser supports RFC 7644 §3.4.2.2 SUBSET (eq, ne, and, or) — rejects co/sw/ew/gt/lt/ge/le with 400 SCIM-compliant error."
    - "GET /.well-known/openid-configuration returns a valid OIDC provider metadata document (issuer, authorization_endpoint, token_endpoint, jwks_uri, etc.)."
    - "OAuth server endpoints: GET /oauth/authorize, POST /oauth/token, POST /oauth/register, GET /oauth/fallback/login (404 when forge.identity.fallback_login=False; 200 with warning when True)."
    - "test_malicious_filter_rejected passes: filter with depth > 8 rejected with 400."
  prohibitions:
    - "MUST NOT support full SCIM filter grammar (D-4 — subset only) — reject co/sw/ew to prevent DoS per Pitfall 5."
    - "MUST NOT enable fallback/login by default — only via explicit admin enable (forge.identity.fallback_login=True)."
    - "MUST NOT return private key in /.well-known/jwks.json (Pitfall 4 + standard JWKS contract)."
    - "MUST NOT allow filter depth > 8 (Pitfall 5 SCIM DoS)."
---

<objective>
Ship the SCIM v2 Groups + ServiceProviderConfig + RFC 7644 SUBSET filter parser + OAuth server surface (authorize/token/register/fallback/login) + OIDC well-known. The Groups + ServiceProviderConfig + OAuth server close out the SCIM + OAuth surfaces for F18.
</objective>

<context>
@.planning/phases/04-expansion-multi-tenant-verification/00-source-spec.md (Feature 18 AC6, AC8)
@.planning/phases/04-expansion-multi-tenant-verification/01-CONTEXT.md (L6 — OAuth server is platform-level)
@.planning/phases/04-expansion-multi-tenant-verification/04-RESEARCH.md (SCIM Provisioning; Pitfall 5 — SCIM filter DoS; Pitfall 8 — agent card leak)

@backend/app/services/phase4_identity.py (after 04-14)
@backend/app/api/v1/forge_phase4/identity.py (after 04-14)
@backend/app/main.py::mount_identity_discovery (after 04-14)
</context>

<tasks>

<task type="auto">
  <name>Task 1: SCIM Groups + ServiceProviderConfig + filter parser + OAuth server + well-known</name>
  <files>backend/app/services/scim_filter_parser.py, backend/app/services/phase4_identity.py, backend/app/api/v1/forge_phase4/identity.py, backend/app/main.py, backend/app/schemas/phase4.py, backend/app/core/phase4_audit_events.py, backend/tests/phase4/test_scim_groups.py, backend/tests/phase4/test_oauth_server.py</files>
  <read_first>
    - backend/app/services/phase4_identity.py (after 04-14)
    - backend/app/api/v1/forge_phase4/identity.py (after 04-14)
    - backend/app/main.py::mount_identity_discovery (after 04-14)
    - backend/app/services/feature_flag_catalog.py::FORGE_IDENTITY_FALLBACK_LOGIN (verify if present)
  </read_first>
  <acceptance_criteria>
    - POST /scim/v2/Groups creates a Group; PATCH /scim/v2/Groups/{id} updates members via add/remove Operations.
    - GET /scim/v2/ServiceProviderConfig returns the canonical SCIM SPConfig.
    - test_malicious_filter_rejected passes: depth > 8 → 400 SCIM-compliant error response {schemas: ['urn:ietf:params:scim:api:messages:2.0:Error'], status: '400', detail: '...'}.
    - GET /.well-known/openid-configuration returns {issuer, authorization_endpoint, token_endpoint, jwks_uri, response_types_supported: ['code'], grant_types_supported: ['authorization_code', 'client_credentials', 'refresh_token'], subject_types_supported: ['public'], id_token_signing_alg_values_supported: ['RS256']}.
    - POST /oauth/token with grant_type=client_credentials + valid client_id+secret returns {access_token, token_type: 'Bearer', expires_in: 3600, scope}.
    - GET /oauth/fallback/login with feature flag disabled returns 403; with enabled returns 200 + warning banner.
    - test_oauth_authorize_code_flow passes: simulated browser redirect → callback → token exchange → access_token.
  </acceptance_criteria>
  <action>
    Create backend/app/services/scim_filter_parser.py with recursive-descent parser for SUBSET (eq, ne, and, or) only; depth cap 8; raises ValueError on co/sw/ew. In backend/app/services/phase4_identity.py, add methods: scim_create_group, scim_get_group, scim_list_groups, scim_patch_group (Operations-based membership diff). In backend/app/api/v1/forge_phase4/identity.py, add the 6 SCIM Group routes + GET /scim/v2/ServiceProviderConfig + GET /.well-known/openid-configuration + GET /oauth/authorize + POST /oauth/token + POST /oauth/register + GET /oauth/fallback/login. In backend/app/main.py::mount_identity_discovery, add /.well-known/openid-configuration as a top-level GET + /oauth/* routes + /scim/v2/ServiceProviderConfig. OAuth authorize code flow: redirect to /oauth/authorize?client_id&redirect_uri&response_type=code&scope&state → if user not logged in, redirect to IdP (SSO) or login page; on callback, exchange code for access_token using Phase4JwtSigningKey. fallback_login route checks feature flag forge.identity.fallback_login — if False returns 403; if True renders simple email/password form (mocked for tests). Add Phase4AuditAction.SCIM_GROUP_CREATED, OAUTH_TOKEN_ISSUED, FALLBACK_LOGIN_USED.
  </action>
  <verify>
    ```bash
    cd /home/arunachalam.v@knackforge.com/forge-ai/backend
    pytest tests/phase4/test_scim_groups.py -x 2>&1 | tail -10
    pytest tests/phase4/test_scim_groups.py::test_malicious_filter_rejected -x 2>&1 | tail -5
    pytest tests/phase4/test_oauth_server.py::test_well_known_openid_configuration -x 2>&1 | tail -5
    pytest tests/phase4/test_oauth_server.py::test_fallback_login_disabled_by_default -x 2>&1 | tail -5
    pytest tests/phase4/test_oauth_server.py::test_client_credentials_grant -x 2>&1 | tail -5
    ```
  </verify>
  <done>SCIM Groups work; filter DoS prevented; OAuth server live; fallback/login disabled by default.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External IdP to Forge OAuth server | Client credentials validated against Phase4OAuthClient table |
| SCIM IdP to Forge /scim/v2/* | Bearer token; filter parser with depth cap |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-15-01 | Denial of Service | SCIM filter DoS via deeply nested expression | high | mitigate | Filter grammar SUBSET; depth cap 8 |
| T-04-15-02 | Elevation of Privilege | fallback/login abused for emergency access | high | mitigate | Disabled by default; audit row FALLBACK_LOGIN_USED per use |
| T-04-15-03 | Spoofing | forged client_credentials | medium | mitigate | Hash client_secret at rest; verify on token request |

</threat_model>

<verification>
- pytest backend/tests/phase4/test_scim_groups.py exits 0
- pytest backend/tests/phase4/test_oauth_server.py exits 0
</verification>

<success_criteria>
- One task committed.
- 04-15-SUMMARY.md written.
- F18 AC6, AC8 both have at least one passing test.
</success_criteria>

<output>
Create .planning/phases/04-expansion-multi-tenant-verification/04-15-SUMMARY.md when done.
</output>

## Artifacts this phase produces

- backend/app/services/scim_filter_parser.py — recursive-descent parser SUBSET (eq/ne/and/or) with depth cap (NEW)
- backend/app/services/phase4_identity.py::scim_create_group, scim_patch_group, etc. — SCIM Groups CRUD
- backend/app/services/phase4_identity.py::oauth_authorize, oauth_token, oauth_register — OAuth server
- backend/app/api/v1/forge_phase4/identity.py::POST/GET/PATCH/DELETE /scim/v2/Groups
- backend/app/api/v1/forge_phase4/identity.py::GET /scim/v2/ServiceProviderConfig
- backend/app/api/v1/forge_phase4/identity.py::GET /.well-known/openid-configuration
- backend/app/api/v1/forge_phase4/identity.py::GET /oauth/authorize, POST /oauth/token, POST /oauth/register, GET /oauth/fallback/login
- backend/app/main.py::mount_identity_discovery — top-level mount for /.well-known and /oauth/*
- backend/app/core/phase4_audit_events.py::Phase4AuditAction.SCIM_GROUP_CREATED, OAUTH_TOKEN_ISSUED, FALLBACK_LOGIN_USED
- env var: FORGE_OAUTH_AUTHORIZE_URL (default to platform URL)
- new Alembic migration: none