---
phase: "04"
plan_id: "04-11"
title: "F17 Realtime — WS /api/forge/realtime handler with JWT auth + 30s grace + 4h soak + compact"
wave: 3
depends_on: ["04-10"]
files_modified:
  - backend/app/api/v1/forge_phase4/sessions.py
  - backend/app/services/phase4_sessions.py
  - backend/tests/phase4/test_sessions_websocket.py
autonomous: true
requirements_addressed:
  - SCALE-F17-AC2
  - SCALE-F17-AC6
  - SCALE-F17-AC9
must_haves:
  truths:
    - "WebSocket /api/forge/realtime accepts JWT via ?token=<jwt> query (matches apps/forge api.ws() pattern)."
    - "test_realtime_session_runs_4h_no_drop passes: with mocked time acceleration, 480 chunks sent at 30s intervals all reach LiteLLM and are echoed back without loss."
    - "Session resume within 30s of disconnect reconnects with full context (heartbeat updated, status='active')."
    - "Session state replicated across Forge Backend instances: connecting to a different replica with the same session_id resumes the same DB-backed session."
    - "POST /api/forge/sessions/{id}/compact reduces token count by > 50% while preserving the most recent 10 messages."
  prohibitions:
    - "MUST NOT use module-level Python dict for active connections (Pitfall 3) — must read state from DB on every reconnect."
    - "MUST NOT silently accept WebSocket upgrade without JWT validation."
    - "MUST NOT compact messages older than the most recent 10."
---

<objective>
Ship the F17 Realtime WebSocket handler + 4-hour soak + compact endpoint. The skeleton sessions.py has router stubs; this plan adds the WebSocket endpoint with JWT auth, chunk-relay logic, 30s reconnect grace, compact endpoint, and multi-replica state replication test.
</objective>

<context>
@.planning/phases/04-expansion-multi-tenant-verification/00-source-spec.md (Feature 17 AC2, AC6, AC9)
@.planning/phases/04-expansion-multi-tenant-verification/01-CONTEXT.md (L5 — 30s grace, multi-replica via Redis pub/sub)
@.planning/phases/04-expansion-multi-tenant-verification/04-RESEARCH.md (Session State Architecture)

@backend/app/api/v1/forge_phase4/sessions.py (after 04-10)
@backend/app/services/phase4_sessions.py (after 04-10)
@apps/forge/CLAUDE.md (api.ws() pattern — backend accepts ?token=<jwt> in WS query)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Realtime WebSocket handler + compact endpoint + 4h soak test + multi-replica resume test</name>
  <files>backend/app/api/v1/forge_phase4/sessions.py, backend/app/services/phase4_sessions.py, backend/tests/phase4/test_sessions_websocket.py</files>
  <read_first>
    - backend/app/api/v1/forge_phase4/sessions.py (after 04-10)
    - backend/app/services/phase4_sessions.py (after 04-10 — uuid7 + state machine)
    - backend/app/main.py (verify WebSocket route mounting)
    - apps/forge/CLAUDE.md (api.ws() pattern — ?token=<jwt>)
  </read_first>
  <acceptance_criteria>
    - WebSocket /api/forge/realtime accepts ?token=<jwt>; rejects without token (closes 1008).
    - On connect: validate JWT, look up Phase4Session by id (from path or query), set status='active', UPDATE last_heartbeat_at, INSERT Phase4SessionEvent.
    - test_realtime_session_runs_4h_no_drop passes: mocked time advances 4h; 480 chunks sent at 30s intervals all relayed.
    - test_resume_within_30s_grace passes: disconnect, reconnect within 30s, status='active', context preserved.
    - test_multi_replica_resume passes: replica A creates session, replica B receives resume with same session_id (mocked two-app fixture).
    - POST /api/forge/sessions/{id}/compact: input {keep_last_n: 10} returns 200 + {original_token_count, compacted_token_count, reduction_pct}; reduction_pct > 50% when session has >20 messages.
  </acceptance_criteria>
  <action>
    In backend/app/api/v1/forge_phase4/sessions.py, add WebSocket route @router.websocket("/realtime"): (1) extract ?token=<jwt> from query_params; (2) decode JWT via Phase4JwtSigningKey public key; on failure await websocket.close(code=1008); (3) extract session_id from path; (4) SELECT Phase4Session WHERE id=session_id AND status IN ('active','disconnected'); (5) UPDATE status='active', last_heartbeat_at=now(); (6) loop: receive_text from client, forward to LiteLLM /v1/realtime (via httpx + aiter_bytes or direct WS); relay responses back; on client disconnect set status='disconnected' + start 30s grace timer. Add POST /sessions/{id}/compact route: input {keep_last_n: 10} — read Phase4SessionEvent rows ordered by created_at DESC limit keep_last_n; replace older events with a single summary event; return token reduction stats (computed via litellm token counting if available, else naive 4-chars-per-token). In backend/app/services/phase4_sessions.py, add method compact(id, tenant_id, project_id, keep_last_n=10). In tests, use freezegun or monkeypatch datetime.now to accelerate time.
  </action>
  <verify>
    ```bash
    cd /home/arunachalam.v@knackforge.com/forge-ai/backend
    pytest tests/phase4/test_sessions_websocket.py::test_realtime_session_runs_4h_no_drop -x 2>&1 | tail -10
    pytest tests/phase4/test_sessions_websocket.py::test_resume_within_30s_grace -x 2>&1 | tail -5
    pytest tests/phase4/test_sessions_websocket.py::test_multi_replica_resume -x 2>&1 | tail -5
    pytest tests/phase4/test_sessions_websocket.py::test_compact_reduces_tokens_over_50_percent -x 2>&1 | tail -5
    ```
  </verify>
  <done>WebSocket realtime works; 4h soak test passes; resume within grace; multi-replica resume works; compact works.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WebSocket client to Forge | JWT in query; verified via Phase4JwtSigningKey |
| Multi-replica state | DB-backed; Redis pub/sub propagates cancel |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-11-01 | Spoofing | WebSocket without JWT | high | mitigate | Validate JWT before accept; close 1008 on missing/invalid |
| T-04-11-02 | Denial of Service | long-lived WS exhausts resources | medium | mitigate | max_duration enforced; auto-expire scheduler |
| T-04-11-03 | Information Disclosure | state in module-level dict | medium | mitigate | DB-backed session state on every reconnect |

</threat_model>

<verification>
- pytest backend/tests/phase4/test_sessions_websocket.py exits 0
</verification>

<success_criteria>
- One task committed.
- 04-11-SUMMARY.md written.
- F17 AC2, AC6, AC9 all have at least one passing test.
</success_criteria>

<output>
Create .planning/phases/04-expansion-multi-tenant-verification/04-11-SUMMARY.md when done.
</output>

## Artifacts this phase produces

- backend/app/api/v1/forge_phase4/sessions.py::WebSocket /realtime — JWT-authed chunk relay
- backend/app/api/v1/forge_phase4/sessions.py::POST /sessions/{id}/compact — token reduction
- backend/app/services/phase4_sessions.py::compact — message compaction helper
- env var: none
- new Alembic migration: none