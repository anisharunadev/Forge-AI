---
phase: "04"
plan_id: "04-07"
title: "F20 Ops/Vault — credentials CRUD + VaultConfig + NEVER-RETURNED test + BrandingTab wiring"
wave: 2
depends_on: ["04-00"]
files_modified:
  - backend/app/services/phase4_ops.py
  - backend/app/api/v1/forge_phase4/ops.py
  - backend/app/schemas/phase4.py
  - backend/app/core/phase4_audit_events.py
  - apps/forge/components/admin/settings/VaultTab.tsx
  - apps/forge/components/admin/settings/BrandingTab.tsx
  - apps/forge/lib/api/credentials.ts
  - apps/forge/hooks/use-credentials.ts
  - backend/tests/phase4/test_credentials_vault.py
  - backend/tests/phase4/test_settings_branding.py
autonomous: true
requirements_addressed:
  - SCALE-F20-AC1
  - SCALE-F20-AC2
  - SCALE-F20-AC6
must_haves:
  truths:
    - "POST /api/forge/credentials with body {credential_name, credential_values, model_id} stores the credential via LiteLLM and the raw value is NEVER returned in any subsequent GET response."
    - "GET /api/forge/credentials/{name} response body does NOT contain any field that could carry the credential_values raw string (verified by response_model_exclude={credential_values} on the route). "
    - "Vault-backed credentials: when Phase4VaultConfig.vault_url is set, credential_values are written to Vault at secret/data/litellm/{provider}/{name} and Forge DB stores only the path reference."
    - "Branding update reflects in admin UI within 60s."
  prohibitions:
    - "MUST NOT store the raw credential value in Phase4Credential."
    - "MUST NOT return credential_values in any GET response shape."
    - "MUST NOT write credential_values to audit_log payload."
    - "MUST NOT skip tenant_id filter on credential lookup (R2)."
---

<objective>
Ship the F20 credentials + vault + branding surfaces. The skeleton phase4_ops.py service exists; this plan wires the credential CRUD routes through LiteLLM Proxy (with VaultConfig for vault-backed storage), enforces the NEVER-RETURNED credential value rule via Pydantic response exclusion, and wires BrandingTab and the new VaultTab UI.
</objective>

<context>
@.planning/phases/04-expansion-multi-tenant-verification/00-source-spec.md (Feature 20 AC1, AC2, AC6)
@.planning/phases/04-expansion-multi-tenant-verification/01-CONTEXT.md (L8 vault-backed; D-5 admin-only)
@.planning/phases/04-expansion-multi-tenant-verification/04-RESEARCH.md (Credentials, Vault, Branding sections)

@backend/app/services/phase4_ops.py (existing skeleton)
@backend/app/api/v1/forge_phase4/ops.py (existing stub router)
@backend/app/db/models/phase4.py::Phase4Credential, Phase4VaultConfig
@backend/app/core/phase4_audit_events.py (CREDENTIALS_ADDED, UPDATED, DELETED, SETTINGS_UPDATED)
@apps/forge/components/admin/settings/BrandingTab.tsx (existing)
@apps/forge/components/admin/settings/VaultTab.tsx (stub from 04-00)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Credential CRUD via LiteLLM + VaultConfig + NEVER-RETURNED enforcement</name>
  <files>backend/app/services/phase4_ops.py, backend/app/api/v1/forge_phase4/ops.py, backend/app/schemas/phase4.py, backend/app/core/phase4_audit_events.py, backend/tests/phase4/test_credentials_vault.py</files>
  <read_first>
    - backend/app/services/phase4_ops.py (existing methods)
    - backend/app/db/models/phase4.py::Phase4Credential, Phase4VaultConfig
    - backend/app/integrations/litellm/litellm_base_client.py (admin_client for /credentials)
    - docs/litellm/forge-litellm-integration.md §2 — /credentials endpoints
  </read_first>
  <acceptance_criteria>
    - POST /api/forge/credentials with body {credential_name, credential_values: {api_key: "sk-secret"}, model_id} returns 200 + CredentialResponse (which contains credential_name + model_id only).
    - GET /api/forge/credentials/{name} returns 200 + CredentialResponse with credential_values excluded via response_model_exclude.
    - Test test_credential_value_never_returned passes: search the entire GET response bytes for the literal string 'sk-secret' that was POSTed, assert 0 matches.
    - When Phase4VaultConfig.vault_url is non-null, upsert_credential() calls VaultClient.put(path=f"secret/data/litellm/{provider}/{name}", data=credential_values) and stores only vault_path in Phase4Credential.
    - When Phase4VaultConfig.vault_url is null, falls through to LiteLLM /credentials.
    - All credential endpoints require require_permission("forge.credentials.admin").
  </acceptance_criteria>
  <action>
    In backend/app/schemas/phase4.py, add Pydantic models: CredentialCreateRequest {credential_name: str, credential_values: dict[str, str], model_id: str | None, provider: str}; CredentialResponse {credential_name: str, model_id: str | None, provider: str, vault_path: str | None, created_at: datetime}. In backend/app/services/phase4_ops.py, modify upsert_credential() to: (1) fetch Phase4VaultConfig for tenant; (2) if vault_url present, call VaultClient.put(path=f"secret/data/litellm/{provider}/{credential_name}", data=credential_values) and INSERT Phase4Credential with vault_path set; (3) else POST to LiteLLM /credentials. NEVER write credential_values to Phase4Credential. Add a thin VaultClient class with put(path, data) / get(path) methods; use in-memory dict for tests. In backend/app/api/v1/forge_phase4/ops.py, add POST /credentials, GET /credentials, GET /credentials/{name}, DELETE /credentials/{name} routes with response_model_exclude={"credential_values"} on each (canonical FastAPI exclusion pattern). Audit CREDENTIALS_ADDED on POST, CREDENTIALS_DELETED on DELETE.
  </action>
  <verify>
    ```bash
    cd /home/arunachalam.v@knackforge.com/forge-ai/backend
    pytest tests/phase4/test_credentials_vault.py::test_credential_value_never_returned -x 2>&1 | tail -10
    pytest tests/phase4/test_credentials_vault.py::test_vault_backed_stores_only_path -x 2>&1 | tail -10
    pytest tests/phase4/test_credentials_vault.py::test_litellm_fallback_when_no_vault -x 2>&1 | tail -10
    ```
  </verify>
  <done>Credential CRUD works; value never returned in GET; vault path stored instead of value.</done>
</task>

<task type="auto">
  <name>Task 2: Branding endpoint + BrandingTab wiring + VaultTab UI</name>
  <files>backend/app/api/v1/forge_phase4/ops.py, backend/app/services/phase4_ops.py, apps/forge/components/admin/settings/BrandingTab.tsx, apps/forge/components/admin/settings/VaultTab.tsx, apps/forge/lib/api/credentials.ts, apps/forge/hooks/use-credentials.ts, backend/tests/phase4/test_settings_branding.py</files>
  <read_first>
    - backend/app/services/phase4_ops.py::get_branding, update_branding (existing or stub)
    - apps/forge/components/admin/settings/BrandingTab.tsx (existing)
    - apps/forge/components/admin/settings/VaultTab.tsx (stub from 04-00)
    - apps/forge/lib/api/client.ts (api.get / api.patch patterns)
  </read_first>
  <acceptance_criteria>
    - PATCH /api/forge/settings/branding with body {logo_url: "...", primary_color: "#abc"} updates Tenant.settings['branding'] JSONB and returns 200 + updated settings.
    - GET /api/forge/settings/branding returns the current branding settings.
    - BrandingTab PATCH call triggers re-render with new values within 60s.
    - VaultTab shows a form with vault_url input and a Test Connection button; POST /api/forge/settings/vault/test returns 200 on success, 503 on connection failure.
    - pnpm --filter forge-dashboard typecheck exits 0.
  </acceptance_criteria>
  <action>
    In backend/app/services/phase4_ops.py, add methods get_branding(tenant_id) and update_branding(tenant_id, branding_dict) that read/write Tenant.settings['branding']. In backend/app/api/v1/forge_phase4/ops.py, add GET /settings/branding and PATCH /settings/branding routes. Add POST /settings/vault/test that creates a VaultClient with given vault_url + token, calls put(path='_test', data={'probe': True}), returns 200 on success. In apps/forge/components/admin/settings/BrandingTab.tsx, wire PATCH on form submit (use TanStack Query useMutation). In apps/forge/components/admin/settings/VaultTab.tsx, replace stub with form (vault_url input + token input + Test Connection button) that calls api.settings.vault.test. Add apps/forge/lib/api/credentials.ts with typed wrappers api.credentials.{list, create, get, delete} and api.settings.{getBranding, updateBranding, testVault}. Add apps/forge/hooks/use-credentials.ts with useCredentials, useCreateCredential, useDeleteCredential, useBranding, useUpdateBranding, useTestVault. Tests cover branding update + vault test connection success/failure.
  </action>
  <verify>
    ```bash
    cd /home/arunachalam.v@knackforge.com/forge-ai/backend
    pytest tests/phase4/test_settings_branding.py -x 2>&1 | tail -10
    pytest tests/phase4/test_credentials_vault.py::test_vault_test_connection -x 2>&1 | tail -5
    cd /home/arunachalam.v@knackforge.com/forge-ai/apps/forge
    pnpm --filter forge-dashboard typecheck 2>&1 | tail -5
    ```
  </verify>
  <done>Branding CRUD works; VaultTab UI live; tenant setting propagation under 60s; typecheck clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin user to credential storage | Plaintext credential value must never leave the server unencrypted |
| Branding update to UI | Tenant settings cache must invalidate so UI reflects within 60s |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-07-01 | Information Disclosure | credential value in GET response | critical | mitigate | response_model_exclude={credential_values} on all credential GET routes |
| T-04-07-02 | Information Disclosure | credential value in audit log | high | mitigate | Audit payload contains only credential_name + model_id, never values |
| T-04-07-03 | Elevation of Privilege | non-admin upserts credential | medium | mitigate | require_permission("forge.credentials.admin") |

</threat_model>

<verification>
- pytest backend/tests/phase4/test_credentials_vault.py exits 0
- pytest backend/tests/phase4/test_settings_branding.py exits 0
- pnpm --filter forge-dashboard typecheck exits 0
- Manual: VaultTab shows form; BrandingTab updates logo_url and the UI re-renders.
</verification>

<success_criteria>
- Two tasks committed.
- 04-07-SUMMARY.md written.
- F20 AC1, AC2, AC6 all have at least one passing test.
</success_criteria>

<output>
Create .planning/phases/04-expansion-multi-tenant-verification/04-07-SUMMARY.md when done.
</output>

## Artifacts this phase produces

- backend/app/services/phase4_ops.py::VaultClient — thin Vault client (in-memory for tests)
- backend/app/services/phase4_ops.py::Phase4OpsService.upsert_credential — vault or LiteLLM dispatch
- backend/app/services/phase4_ops.py::Phase4OpsService.get_branding, update_branding — branding CRUD
- backend/app/api/v1/forge_phase4/ops.py::POST /credentials, GET /credentials, GET /credentials/{name}, DELETE /credentials/{name}
- backend/app/api/v1/forge_phase4/ops.py::GET /settings/branding, PATCH /settings/branding
- backend/app/api/v1/forge_phase4/ops.py::POST /settings/vault/test
- backend/app/schemas/phase4.py::CredentialCreateRequest, CredentialResponse
- apps/forge/components/admin/settings/BrandingTab.tsx::BrandingTab — wired with PATCH
- apps/forge/components/admin/settings/VaultTab.tsx::VaultTab — form + test connection
- apps/forge/lib/api/credentials.ts — typed API wrappers
- apps/forge/hooks/use-credentials.ts — TanStack Query hooks
- env var: HASHICORP_VAULT_URL, HASHICORP_VAULT_TOKEN (optional — only used if Phase4VaultConfig row exists)
- new Alembic migration: none (Phase4VaultConfig + Phase4Credential tables from 04-00)