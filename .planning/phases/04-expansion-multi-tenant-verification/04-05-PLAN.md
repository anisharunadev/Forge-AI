---
phase: "04"
plan_id: "04-05"
title: "F16 Pass-through — SSE byte-identical streaming + Bedrock SigV4 + Vertex IAM + provider mounts"
wave: 2
depends_on: ["04-04"]
files_modified:
  - backend/app/main.py
  - backend/tests/phase4/test_passthrough_stream.py
  - backend/tests/phase4/test_passthrough_headers.py
  - backend/tests/phase4/test_passthrough_mounts.py
autonomous: true
requirements_addressed:
  - SCALE-F16-AC3
  - SCALE-F16-AC4
  - SCALE-F16-AC5
must_haves:
  truths:
    - "POST /openai/v1/chat/completions with stream=true returns SSE chunks byte-identical to the LiteLLM response (no Forge-specific fields inserted)."
    - "POST /anthropic/v1/messages with anthropic-version header returns an Anthropic-format response (no OpenAI shape forced)."
    - "POST /bedrock/invoke with x-amz-date, x-amz-security-token, authorization-credential headers preserves those headers verbatim through pass-through."
    - "POST /vertex_ai/... with x-goog-api-version + authorization-credential preserves those headers."
    - "mount_passthrough() mounts all 12+ provider surfaces at app root."
  prohibitions:
    - "MUST NOT buffer the entire SSE response in memory (must yield chunks verbatim via httpx.AsyncClient.stream)."
    - "MUST NOT strip x-amz-* or x-goog-* headers even though they are vendor-specific."
    - "MUST NOT add a Forge envelope to non-JSON bodies."
---

<objective>
Close the F16 streaming + provider-shape preservation acceptance criteria. Verify that SSE chunks from LiteLLM reach the client byte-identically, that vendor-specific headers (SigV4 for Bedrock, IAM for Vertex) survive the pass-through header allowlist, and that all 12+ provider surfaces are mounted at app root via mount_passthrough().
</objective>

<context>
@.planning/phases/04-expansion-multi-tenant-verification/00-source-spec.md (Feature 16 AC3, AC4, AC5)
@.planning/phases/04-expansion-multi-tenant-verification/01-CONTEXT.md (L3 wire-transparent)
@.planning/phases/04-expansion-multi-tenant-verification/04-RESEARCH.md (Pass-Through Architecture, Bedrock SigV4)

@backend/app/api/v1/forge_phase4/passthrough.py (after 04-04)
@backend/app/services/phase4_providers.py::PROVIDERS
@backend/app/integrations/litellm/pass_through.py::_PROVIDER_PASSTHROUGH_HEADER_ALLOWLIST
@backend/app/main.py::mount_phase4_top_level
</context>

<tasks>

<task type="auto">
  <name>Task 1: SSE byte-identical streaming + Anthropic format + Bedrock/Vertex header preservation tests + mount verification</name>
  <files>backend/tests/phase4/test_passthrough_stream.py, backend/tests/phase4/test_passthrough_headers.py, backend/tests/phase4/test_passthrough_mounts.py, backend/app/main.py</files>
  <read_first>
    - backend/app/api/v1/forge_phase4/passthrough.py::mount_passthrough (after 04-04)
    - backend/app/services/phase4_providers.py::PROVIDERS (all entries)
    - backend/app/main.py::mount_phase4_top_level call site
    - backend/app/integrations/litellm/pass_through.py::_PROVIDER_PASSTHROUGH_HEADER_ALLOWLIST
  </read_first>
  <acceptance_criteria>
    - test_sse_byte_identical passes: mocked httpx returns 5 SSE chunks; client receives exactly those 5 chunks in the same order with byte-identical bytes.
    - test_anthropic_messages_preserves_format passes: POST /anthropic/v1/messages returns Anthropic-shaped response (type=message, content array) — not coerced to OpenAI shape.
    - test_bedrock_sigv4_headers_preserved passes: client request with x-amz-date, x-amz-security-token, authorization-credential headers; mock transport asserts those exact headers in the outbound request.
    - test_vertex_iam_headers_preserved passes: same pattern for x-goog-api-version + authorization-credential.
    - test_mount_passthrough_registers_all_providers passes: app.routes contains a path matching every PROVIDERS upstream prefix (/openai/{path:path}, /anthropic/{path:path}, /bedrock/{path:path}, /vertex_ai/{path:path}, /gemini/{path:path}, etc.).
    - test_anthropic_root_alias passes: POST /v1/messages (canonical Anthropic Messages path) routes to the same handler as POST /anthropic/v1/messages.
  </acceptance_criteria>
  <action>
    In backend/tests/phase4/test_passthrough_stream.py, write test_sse_byte_identical: mock httpx.MockTransport to return a stream with 5 chunks ['data: {chunk1}\\n\\n', ...]; patch the LiteLLM client to use the mock; POST /openai/v1/chat/completions with Accept: text/event-stream and stream=true body; collect response chunks via httpx.AsyncClient; assert bytewise equality. Write test_anthropic_messages_preserves_format: mock returns {"type": "message", "content": [{"type": "text", "text": "..."}], ...}; POST /anthropic/v1/messages; assert response.json()["type"] == "message" (Anthropic shape). In backend/tests/phase4/test_passthrough_headers.py, write test_bedrock_sigv4_headers_preserved: mock transport's request handler captures the outbound request's headers dict; POST /bedrock/invoke with x-amz-date, x-amz-security-token, authorization-credential headers; assert mock_received["headers"] contains all three. Same for test_vertex_iam_headers_preserved with x-goog-api-version. In backend/tests/phase4/test_passthrough_mounts.py, write test_mount_passthrough_registers_all_providers: import the FastAPI app from app.main; iterate app.routes; assert every PROVIDERS upstream prefix has a matching path like {prefix}/{path:path}. Write test_anthropic_root_alias: assert /v1/messages and /anthropic/v1/messages both exist as POST routes. In backend/app/main.py, verify mount_phase4_top_level(app) is called AFTER app.include_router(v1_router); if missing, add it.
  </action>
  <verify>
    ```bash
    cd /home/arunachalam.v@knackforge.com/forge-ai/backend
    pytest tests/phase4/test_passthrough_stream.py -x 2>&1 | tail -10
    pytest tests/phase4/test_passthrough_headers.py -x 2>&1 | tail -10
    pytest tests/phase4/test_passthrough_mounts.py -x 2>&1 | tail -10
    ```
  </verify>
  <done>SSE byte-identical; Anthropic + Bedrock + Vertex preservation verified; all provider mounts live.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LiteLLM to upstream provider | Vendor-specific headers (SigV4, IAM) must survive header allowlist |
| Streaming client to LiteLLM | Bytes-in-bytes-out; no Forge envelope for non-JSON bodies |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-05-01 | Tampering | Bedrock rejects mismatched x-amz-* headers | high | mitigate | _PROVIDER_PASSTHROUGH_HEADER_ALLOWLIST includes all x-amz-* prefixes |
| T-04-05-02 | Denial of Service | SSE buffering exhausts memory | medium | mitigate | httpx.AsyncClient.stream + aiter_bytes |
| T-04-05-03 | Spoofing | Vertex rejects stale x-goog-api-version | low | mitigate | Allowlist preserves x-goog-* prefixes |

</threat_model>

<verification>
- pytest backend/tests/phase4/test_passthrough_stream.py exits 0
- pytest backend/tests/phase4/test_passthrough_headers.py exits 0
- pytest backend/tests/phase4/test_passthrough_mounts.py exits 0
</verification>

<success_criteria>
- One task committed.
- 04-05-SUMMARY.md written.
- F16 AC3, AC4, AC5 all have at least one passing test.
</success_criteria>

<output>
Create .planning/phases/04-expansion-multi-tenant-verification/04-05-SUMMARY.md when done.
</output>

## Artifacts this phase produces

- backend/app/main.py::mount_phase4_top_level — verified to register all provider mounts at app root
- backend/tests/phase4/test_passthrough_stream.py::test_sse_byte_identical, test_anthropic_messages_preserves_format
- backend/tests/phase4/test_passthrough_headers.py::test_bedrock_sigv4_headers_preserved, test_vertex_iam_headers_preserved
- backend/tests/phase4/test_passthrough_mounts.py::test_mount_passthrough_registers_all_providers, test_anthropic_root_alias
- env var: none
- new Alembic migration: none