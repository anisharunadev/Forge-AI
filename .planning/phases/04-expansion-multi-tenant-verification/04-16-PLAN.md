---
phase: "04"
plan_id: "04-16"
title: "F18 JWT signing + JWKS + key rotation + MCP-JWT issuance test"
wave: 4
depends_on: ["04-15"]
files_modified:
  - backend/app/services/phase4_identity.py
  - backend/app/api/v1/forge_phase4/identity.py
  - backend/app/main.py
  - backend/app/core/crypto.py
  - backend/tests/phase4/test_jwt_keys_rotation.py
autonomous: true
requirements_addressed:
  - SCALE-F18-AC5
  - SCALE-F18-AC7
  - SCALE-F18-AC9
must_haves:
  truths:
    - "GET /.well-known/jwks.json returns at least one active Phase4JwtSigningKey's public key in JWKS format {keys: [{kty: 'RSA', kid, use: 'sig', alg: 'RS256', n, e}]}."
    - "POST /api/forge/identity/jwt/keys (admin) creates a new Phase4JwtSigningKey with status='active' and marks the old key status='retired' (NOT deleted) — supports two-phase rotation."
    - "JWKS endpoint emits BOTH active and retired keys for the in-flight token TTL window (default 1h after rotation); after that, retired key is hard-deleted."
    - "Test test_mcp_server_verifies_forge_jwt passes: Forge issues a JWT; an external MCP server fetches /.well-known/jwks.json, picks the right kid, verifies the JWT signature."
    - "Test test_rotation_does_not_invalidate_in_flight_tokens passes: 10 JWTs issued under kid_A; rotate (kid_B becomes active); all 10 JWTs still verify via JWKS (which still contains kid_A until grace expires)."
  prohibitions:
    - "MUST NOT delete the retired key immediately on rotation — must wait for in-flight token TTL (Pitfall 4 — eager cleanup breaks in-flight verification)."
    - "MUST NOT include private key material in JWKS response (only n, e, kid, use, alg, kty)."
    - "MUST NOT require kid in JWT unless JWKS has >1 key (default: kid optional, validated when present)."
---

<objective>
Ship the JWT signing key infrastructure + JWKS endpoint + key rotation + MCP-JWT issuance test. This closes out F18 with the cross-AI MCP verification acceptance criterion (F18 AC7).
</objective>

<context>
@.planning/phases/04-expansion-multi-tenant-verification/00-source-spec.md (Feature 18 AC5, AC7, AC9)
@.planning/phases/04-expansion-multi-tenant-verification/01-CONTEXT.md (L6 — token TTLs; access 1h, refresh 30d rotating)
@.planning/phases/04-expansion-multi-tenant-verification/04-RESEARCH.md (Pitfall 4 — JWT rotation; JWT Signing Keys)

@backend/app/services/phase4_identity.py (after 04-15)
@backend/app/api/v1/forge_phase4/identity.py (after 04-15)
@backend/app/main.py::mount_identity_discovery (after 04-15)
@backend/app/db/models/phase4.py::Phase4JwtSigningKey
@backend/app/core/crypto.py (existing crypto helpers)
</context>

<tasks>

<task type="auto">
  <name>Task 1: JWKS endpoint + key rotation + MCP-JWT verification test</name>
  <files>backend/app/services/phase4_identity.py, backend/app/api/v1/forge_phase4/identity.py, backend/app/main.py, backend/app/core/crypto.py, backend/tests/phase4/test_jwt_keys_rotation.py</files>
  <read_first>
    - backend/app/services/phase4_identity.py (after 04-15)
    - backend/app/db/models/phase4.py::Phase4JwtSigningKey (status enum: active/retired/revoked)
    - backend/app/main.py::mount_identity_discovery (after 04-15)
    - backend/app/core/crypto.py (existing crypto helpers — add RS256 keypair generation if missing)
  </read_first>
  <acceptance_criteria>
    - GET /.well-known/jwks.json returns 200 + {keys: [{kty, kid, use, alg, n, e}]} with at least 1 active key.
    - POST /api/forge/identity/jwt/keys (admin only) creates a new RSA keypair, INSERTs Phase4JwtSigningKey with status='active' + kid='<uuid>'; marks all other active keys status='retired'.
    - JWKS endpoint filters Phase4JwtSigningKey rows WHERE status IN ('active', 'retired') AND (status='active' OR (status='retired' AND created_at > now() - INTERVAL '1 hour')).
    - test_mcp_server_verifies_forge_jwt passes: external MCP fetches /.well-known/jwks.json, picks kid, verifies Forge JWT signature.
    - test_rotation_does_not_invalidate_in_flight_tokens passes: 10 JWTs under kid_A, rotate to kid_B, all 10 still verify (kid_A still in JWKS during grace).
    - Hard-delete scheduler removes retired keys older than 1 hour.
  </acceptance_criteria>
  <action>
    In backend/app/core/crypto.py, add generate_rsa_keypair() returning (private_pem, public_jwk_dict). In backend/app/services/phase4_identity.py, add methods: create_jwt_key() — generates RSA keypair, INSERTs Phase4JwtSigningKey with status='active' + kid=uuid.uuid4().hex; marks all other active keys status='retired'. rotate_jwt_keys() — calls create_jwt_key, returns new kid. jwks() — SELECT Phase4JwtSigningKey WHERE status='active' OR (status='retired' AND created_at > now() - INTERVAL '1 hour'); for each row, build JWKS entry {kty: 'RSA', kid, use: 'sig', alg: 'RS256', n: <base64url-encoded modulus>, e: <base64url-encoded exponent>}; return {keys: [...]}. In backend/app/api/v1/forge_phase4/identity.py, add GET /.well-known/jwks.json (note: this is the canonical OIDC path — mount in main.py at app root, not under /api/forge), POST /api/forge/identity/jwt/keys (admin). In backend/app/main.py::mount_identity_discovery, add GET /.well-known/jwks.json. Add hard-delete cron: backend/app/services/scheduler/jwt_key_cleanup.py with daily cleanup of Phase4JwtSigningKey WHERE status='retired' AND created_at < now() - INTERVAL '1 hour'. Add Phase4AuditAction.JWT_KEY_ROTATED, JWT_KEY_CREATED.
  </action>
  <verify>
    ```bash
    cd /home/arunachalam.v@knackforge.com/forge-ai/backend
    pytest tests/phase4/test_jwt_keys_rotation.py::test_jwks_has_active_key -x 2>&1 | tail -5
    pytest tests/phase4/test_jwt_keys_rotation.py::test_rotation_does_not_invalidate_in_flight_tokens -x 2>&1 | tail -10
    pytest tests/phase4/test_jwt_keys_rotation.py::test_mcp_server_verifies_forge_jwt -x 2>&1 | tail -10
    ```
  </verify>
  <done>JWKS endpoint live; rotation preserves in-flight tokens; MCP JWT verification works.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External MCP to Forge JWKS | Public keys only; no private material |
| Admin to key rotation | Privileged operation; require_permission gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-16-01 | Information Disclosure | private key in JWKS | critical | mitigate | JWKS emits only n, e, kid, use, alg, kty — never private material |
| T-04-16-02 | Repudiation | rotation invalidates in-flight tokens | high | mitigate | Two-phase rotation; retired key retained for 1h grace |

</threat_model>

<verification>
- pytest backend/tests/phase4/test_jwt_keys_rotation.py exits 0
- curl http://localhost:8000/.well-known/jwks.json does NOT contain 'private', 'BEGIN PRIVATE', or 'd' (RSA private exponent) substrings (manual)
</verification>

<success_criteria>
- One task committed.
- 04-16-SUMMARY.md written.
- F18 AC5, AC7, AC9 all have at least one passing test.
</success_criteria>

<output>
Create .planning/phases/04-expansion-multi-tenant-verification/04-16-SUMMARY.md when done.
</output>

## Artifacts this phase produces

- backend/app/core/crypto.py::generate_rsa_keypair — RS256 keypair generator
- backend/app/services/phase4_identity.py::create_jwt_key, rotate_jwt_keys, jwks — JWT key lifecycle
- backend/app/api/v1/forge_phase4/identity.py::POST /jwt/keys — rotation endpoint
- backend/app/main.py::mount_identity_discovery — top-level /jwks.json mount
- backend/app/services/scheduler/jwt_key_cleanup.py::daily_cleanup — retired key hard-delete (NEW)
- backend/app/core/phase4_audit_events.py::Phase4AuditAction.JWT_KEY_CREATED, JWT_KEY_ROTATED
- env var: none (uses existing core/crypto.py for key storage)
- new Alembic migration: none