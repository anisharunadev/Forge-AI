---
phase: "04"
plan_id: "04-12"
title: "F17 A2A — /a2a/.well-known agent card + /a2a/message WS handler + delegation JWT + handshake test"
wave: 3
depends_on: ["04-10"]
files_modified:
  - backend/app/api/v1/forge_phase4/sessions.py
  - backend/app/services/phase4_sessions.py
  - backend/app/services/phase4_identity.py
  - backend/tests/phase4/test_a2a_handshake.py
autonomous: true
requirements_addressed:
  - SCALE-F17-AC3
  - SCALE-F17-AC7
must_haves:
  truths:
    - "GET /a2a/.well-known returns a JSON agent card with: agent_id, supported_capabilities, auth_url — does NOT include any secret, SCIM token, OAuth client secret, or credential value (Pitfall 8). "
    - "WebSocket /a2a/message accepts connection from another agent with a Forge-issued JWT in ?token=<jwt>; completes the handshake by exchanging capability announcements; session row created with type='a2a'."
    - "test_a2a_handshake_between_two_agents passes: mock agent B connects with JWT; mock agent A accepts; both sides record handshake event; Phase4Session row has type='a2a' with delegation_jwt claim stored."
    - "Test test_agent_card_has_no_secret passes: response body string does not contain 'sk-', 'Bearer ', 'client_secret', or 'scim_token' substrings."
  prohibitions:
    - "MUST NOT include any secret in the /a2a/.well-known response (Pitfall 8 — agent card leak)."
    - "MUST NOT skip JWT validation on /a2a/message (must be a Forge-issued JWT verified against /.well-known/jwks.json)."
    - "MUST NOT create Phase4A2ADelegation row without tenant_id + project_id (R2)."
---

<objective>
Ship the A2A handshake: agent card at /a2a/.well-known (with no secrets), WebSocket /a2a/message handler with Forge-JWT auth, delegation JWT issuance, and the handshake integration test.
</objective>

<context>
@.planning/phases/04-expansion-multi-tenant-verification/00-source-spec.md (Feature 17 AC3, AC7)
@.planning/phases/04-expansion-multi-tenant-verification/01-CONTEXT.md (L6 — per-tenant; A2A delegation JWT)
@.planning/phases/04-expansion-multi-tenant-verification/04-RESEARCH.md (A2A section; Pitfall 8 — agent card leak)

@backend/app/api/v1/forge_phase4/sessions.py (after 04-10, 04-11)
@backend/app/services/phase4_sessions.py (after 04-10)
@backend/app/db/models/phase4.py::Phase4A2ADelegation
@backend/app/services/phase4_identity.py (existing JWT issuance)
@backend/app/main.py::mount_a2a (existing — verify it's called)
</context>

<tasks>

<task type="auto">
  <name>Task 1: A2A agent card + WS message handler + delegation JWT + handshake test</name>
  <files>backend/app/api/v1/forge_phase4/sessions.py, backend/app/services/phase4_sessions.py, backend/app/services/phase4_identity.py, backend/tests/phase4/test_a2a_handshake.py</files>
  <read_first>
    - backend/app/api/v1/forge_phase4/sessions.py (after 04-11)
    - backend/app/services/phase4_identity.py (existing JWT issuance)
    - backend/app/main.py::mount_a2a
    - backend/app/db/models/phase4.py::Phase4A2ADelegation
  </read_first>
  <acceptance_criteria>
    - GET /a2a/.well-known returns 200 + {agent_id, supported_capabilities: [...], auth_url, version}. NO secrets present.
    - test_agent_card_has_no_secret passes: response body does NOT contain 'sk-', 'Bearer ', 'client_secret', 'scim_token' substrings.
    - WebSocket /a2a/message accepts ?token=<jwt> where jwt is Forge-issued and signed by Phase4JwtSigningKey. Rejects with 1008 otherwise.
    - On handshake: INSERT Phase4A2ADelegation row with (tenant_id, project_id, agent_a_id, agent_b_id, delegation_jwt_hash, expires_at); create Phase4Session with type='a2a'.
    - test_a2a_handshake_between_two_agents passes.
  </acceptance_criteria>
  <action>
    In backend/app/api/v1/forge_phase4/sessions.py, add mount_a2a helper that registers: GET /a2a/.well-known → returns static JSON {agent_id: 'forge-platform-v2', supported_capabilities: ['chat', 'completion', 'embedding', 'realtime'], auth_url: '/.well-known/openid-configuration', version: '2.0.0'}. NO secrets. WebSocket /a2a/message → validate JWT (from query param or Authorization header), INSERT Phase4A2ADelegation row, create Phase4Session with type='a2a', MAX_DURATIONS['a2a']=3600s, relay messages between the two agents. In backend/app/services/phase4_identity.py, add mint_delegation_jwt(agent_a_id, agent_b_id, tenant_id, project_id, scope, ttl_seconds=3600) that uses the active Phase4JwtSigningKey to sign a JWT with claims {sub: agent_b_id, aud: 'a2a:agent_a_id', scope, forge_tenant_id, forge_project_id}. In backend/tests/phase4/test_a2a_handshake.py, write test_a2a_handshake_between_two_agents: mock agent A and B; B connects with delegation_jwt; A accepts; both sides see the handshake completion event.
  </action>
  <verify>
    ```bash
    cd /home/arunachalam.v@knackforge.com/forge-ai/backend
    pytest tests/phase4/test_a2a_handshake.py::test_agent_card_has_no_secret -x 2>&1 | tail -5
    pytest tests/phase4/test_a2a_handshake.py::test_a2a_handshake_between_two_agents -x 2>&1 | tail -10
    ```
  </verify>
  <done>Agent card published without secrets; A2A handshake works with delegation JWT.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External agent to Forge A2A | JWT verified against Phase4JwtSigningKey public key |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-12-01 | Information Disclosure | agent card leaks secret | high | mitigate | Static JSON with only public fields; tested via substring search |
| T-04-12-02 | Spoofing | forged delegation JWT | high | mitigate | Verify against Phase4JwtSigningKey public key |

</threat_model>

<verification>
- pytest backend/tests/phase4/test_a2a_handshake.py exits 0
- curl http://localhost:8000/a2a/.well-known does NOT contain 'secret', 'token', 'Bearer' substrings (manual)
</verification>

<success_criteria>
- One task committed.
- 04-12-SUMMARY.md written.
- F17 AC3, AC7 both have at least one passing test.
</success_criteria>

<output>
Create .planning/phases/04-expansion-multi-tenant-verification/04-12-SUMMARY.md when done.
</output>

## Artifacts this phase produces

- backend/app/api/v1/forge_phase4/sessions.py::mount_a2a — agent card + WS message handler
- backend/app/services/phase4_identity.py::mint_delegation_jwt — Forge-issued delegation JWT
- env var: none
- new Alembic migration: none