---
plan: 00-01
phase: 0
wave: 1
depends_on: []
files_modified:
  - .claude/CLAUDE.md
  - docs/architecture/overview.md
autonomous: true
requirements: [HYG-01]
---

<objective>
Reconcile the Tailwind version drift in two documentation files so they declare "Tailwind 3.4.x" matching the installed `apps/forge/package.json` pin (`tailwindcss: 3.4.14`). No code, no package manifest, no PostCSS / Tailwind config changes. Add a one-line note to each file stating Tailwind 4 migration is deferred to post-pilot so the next reader does not re-flag the same drift.

Purpose: Eliminate the "Tailwind 4 declared vs 3.4.14 installed" drift before any downstream plan-phase work begins. The drift was already documented as a Phase 0 fix target in `PROJECT.md` and as a locked decision in `STATE.md`. This plan closes it.

Output: Two updated doc files with `Tailwind CSS 3.4.x` and a deferral note.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-pre-roadmap-hygiene/00-RESEARCH.md
@.planning/phases/00-pre-roadmap-hygiene/00-VALIDATION.md
</context>

<must_haves>
truths:
  - ".claude/CLAUDE.md line 36 declares `Tailwind CSS 3.4.x` (was `Tailwind CSS 4`)"
  - "docs/architecture/overview.md line 29 declares `Tailwind CSS 3.4.x` (was `Tailwind CSS 4`)"
  - "Both files include a one-line note stating Tailwind 4 migration is deferred to post-pilot (cross-ref REQUIREMENTS.md Out of Scope)"
  - "`apps/forge/package.json` still pins `tailwindcss: 3.4.14` (no manifest change)"
  - "Zero occurrences of the string `Tailwind CSS 4` remain in either updated file"
artifacts:
  - path: ".claude/CLAUDE.md"
    provides: "Updated Frontend stack block declaring Tailwind 3.4.x + deferral note"
  - path: "docs/architecture/overview.md"
    provides: "Updated Frontend row in Tech Stack table declaring Tailwind 3.4.x + deferral note"
</must_haves>

<tasks>

<task type="auto">
  <name>Task 1: Update CLAUDE.md Frontend block to Tailwind 3.4.x</name>
  <read_first>
    /home/arunachalam.v@knackforge.com/forge-ai/.claude/CLAUDE.md
    /home/arunachalam.v@knackforge.com/forge-ai/apps/forge/package.json
  </read_first>
  <files>.claude/CLAUDE.md</files>
  <action>
    Edit `/home/arunachalam.v@knackforge.com/forge-ai/.claude/CLAUDE.md` Frontend code block (around line 36). Two precise edits in the same fenced code block:

    1. Replace the literal string `Tailwind CSS 4` with `Tailwind CSS 3.4.x` (preserves the surrounding `Next.js 15 / React 19 / TypeScript 5.x / Shadcn/UI` stack list — only the Tailwind version changes).
    2. Append a one-line note immediately after the Frontend block (still inside the Technology Stack section, not inside the fenced code block): `> Note: Tailwind 4 migration is deferred to post-pilot (see REQUIREMENTS.md "Out of Scope" — Tailwind 4 migration). Pinned at 3.4.14 in apps/forge/package.json.`

    Do NOT touch any other technology line, do NOT edit the Critical Implementation Rules section, do NOT add new dependencies, do NOT touch the Tailwind pin in `apps/forge/package.json` (it is already 3.4.14).

    Why a note (and not just a version bump): the next reader who scans the stack will see `3.4.x` and may try to "modernize" it. The note makes the deferral intent unambiguous.
  </action>
  <verify>
    <automated>grep -nE 'Tailwind (CSS )?3\.4\.x' /home/arunachalam.v@knackforge.com/forge-ai/.claude/CLAUDE.md</automated>
    <automated>! grep -nE 'Tailwind (CSS )?4[^.]' /home/arunachalam.v@knackforge.com/forge-ai/.claude/CLAUDE.md</automated>
    <automated>grep -n 'Tailwind 4 migration' /home/arunachalam.v@knackforge.com/forge-ai/.claude/CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE 'Tailwind (CSS )?3\.4\.x' .claude/CLAUDE.md` returns exactly one matching line (the updated Frontend block)
    - `! grep -nE 'Tailwind (CSS )?4[^.]' .claude/CLAUDE.md` returns exit 0 (no `Tailwind 4` without a `.` directly after — the new "Tailwind 4 migration" phrase is allowed because the next char is a space, not a digit/punctuation that closes a version)
    - `grep -n 'Tailwind 4 migration' .claude/CLAUDE.md` returns one matching line (the deferral note)
    - `apps/forge/package.json` is byte-identical to its pre-plan state (`git diff apps/forge/package.json` shows no changes)
  </acceptance_criteria>
  <done>
    CLAUDE.md Frontend block reads `Tailwind CSS 3.4.x` and is followed by a one-line deferral note. No other content in CLAUDE.md changed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update docs/architecture/overview.md Frontend row to Tailwind 3.4.x</name>
  <read_first>
    /home/arunachalam.v@knackforge.com/forge-ai/docs/architecture/overview.md
    /home/arunachalam.v@knackforge.com/forge-ai/apps/forge/package.json
  </read_first>
  <files>docs/architecture/overview.md</files>
  <action>
    Edit `/home/arunachalam.v@knackforge.com/forge-ai/docs/architecture/overview.md` Tech Stack table Frontend row (around line 29). Two precise edits:

    1. In the row `| Frontend | Next.js 15, React 19, TypeScript 5.x, Shadcn/UI, Tailwind CSS 4 | project-context.md |`, replace `Tailwind CSS 4` with `Tailwind CSS 3.4.x`. The row separator, the rest of the cell content, and the `| project-context.md |` source reference all stay unchanged.
    2. Add a one-line note immediately after the Tech Stack table (NOT inside the table — append a paragraph below it): `**Note:** Tailwind 4 migration is deferred to post-pilot (see `REQUIREMENTS.md` "Out of Scope" — `Tailwind 4 migration`). Pinned at `3.4.14` in `apps/forge/package.json`.`

    Do NOT touch the Mission, Constitution, Topology Diagram, Layer Isolation Model, Data Flow, or Cross-References sections. Do NOT add new dependencies. Do NOT touch the Tailwind pin.

    Why a markdown paragraph (and not a footnote): the note must be visible to anyone scrolling past the table, not buried in a footnote. Same intent as the CLAUDE.md note, expressed as a paragraph.
  </action>
  <verify>
    <automated>grep -nE 'Tailwind (CSS )?3\.4\.x' /home/arunachalam.v@knackforge.com/forge-ai/docs/architecture/overview.md</automated>
    <automated>! grep -nE 'Tailwind (CSS )?4[^.]' /home/arunachalam.v@knackforge.com/forge-ai/docs/architecture/overview.md</automated>
    <automated>grep -n 'Tailwind 4 migration' /home/arunachalam.v@knackforge.com/forge-ai/docs/architecture/overview.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE 'Tailwind (CSS )?3\.4\.x' docs/architecture/overview.md` returns exactly one matching line (the updated Frontend row in the Tech Stack table)
    - `! grep -nE 'Tailwind (CSS )?4[^.]' docs/architecture/overview.md` returns exit 0
    - `grep -n 'Tailwind 4 migration' docs/architecture/overview.md` returns one matching line (the deferral note paragraph)
    - The Tech Stack table still has the same number of rows and column separators as before
  </acceptance_criteria>
  <done>
    overview.md Frontend row reads `Tailwind CSS 3.4.x` and is followed by a one-line deferral note paragraph. The Mission, Constitution, and other architecture sections are unchanged.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs→planner assumption | Downstream plan-phase reads CLAUDE.md + overview.md to decide Tailwind version class names and PostCSS pipeline shape. A wrong version line here will silently bias every Phase 1+ plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-01-1 | Information Disclosure | docs stack declaration | accept | The drift itself was the threat — the deliverable resolves it. No new trust boundary. |
| T-00-01-2 | Tampering | docs stack declaration | mitigate | A future PR could re-introduce `Tailwind CSS 4` in either file. Mitigation: the `VERIFY` step in both tasks runs `grep` against the forbidden string. If CI later adds the same gate (HYG-03 only covers UUIDs and `import litellm`; a Tailwind grep gate is out of scope for Phase 0), drift is caught at PR time. |
</threat_model>

<verification>
Run the full Phase 0 HYG-01 verification block from `00-VALIDATION.md`:

```bash
# HYG-01
grep -E "Tailwind (CSS )?3\.4\.x" .claude/CLAUDE.md docs/architecture/overview.md   # both match
! grep -E "Tailwind (CSS )?4" .claude/CLAUDE.md docs/architecture/overview.md       # zero matches
grep '"tailwindcss"' apps/forge/package.json                                         # "3.4.14" still pinned
```

All three commands must succeed. Then run the per-task greps from `<verify>` blocks.
</verification>

<success_criteria>
HYG-01 success criterion from ROADMAP.md is satisfied: "CLAUDE.md and docs/architecture/overview.md declare 'Tailwind 3.4.x' matching the installed apps/forge/package.json pin (no more 3.4.14 vs 4 drift)."

Measurable:
- `grep "Tailwind CSS 3.4.x" .claude/CLAUDE.md docs/architecture/overview.md` returns 2 matching lines (one per file)
- `! grep "Tailwind CSS 4" .claude/CLAUDE.md docs/architecture/overview.md` returns exit 0
- `grep "tailwindcss" apps/forge/package.json` shows `"tailwindcss": "3.4.14"` (unchanged)
- Both files contain the phrase "Tailwind 4 migration" in a deferral note
- `git diff --stat` shows exactly 2 files modified: `.claude/CLAUDE.md` and `docs/architecture/overview.md`
</success_criteria>

<output>
Create `.planning/phases/00-pre-roadmap-hygiene/00-01-SUMMARY.md` when done
</output>
