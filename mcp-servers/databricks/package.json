{
  "name": "forge-ai/mcp-databricks",
  "version": "0.1.0",
  "description": "FORA Databricks MCP server — exposes list_jobs, get_job, run_job, get_run, cancel_run, list_clusters, get_cluster, execute_sql over MCP/stdio. Auth via a workspace-scoped service-principal PAT against the Databricks Jobs REST 2.1 + SQL Statement Execution APIs.",
  "license": "UNLICENSED",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "fora-mcp-databricks": "bin/fora-mcp-databricks.mjs"
  },
  "files": [
    "dist",
    "bin",
    "README.md",
    "docs"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "prepare": "npm run build",
    "start": "node bin/fora-mcp-databricks.mjs",
    "smoke": "node test/smoke.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "engines": {
    "node": ">=18.17"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^26.0.0",
    "typescript": "^6.0.3"
  }
}
