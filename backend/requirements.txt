# Forge AI backend — pinned for Python 3.13.
# Rule 1 (NO direct LLM SDKs): we use httpx to talk to the LiteLLM
# Proxy; openai/anthropic/google-generativeai are NOT direct deps.

fastapi>=0.115,<0.117
uvicorn[standard]>=0.30,<0.33
sqlalchemy[asyncio]>=2.0,<2.1
asyncpg>=0.29,<0.31
psycopg2-binary>=2.9,<3  # sync DBAPI used by SQLAlchemy pool_pre_ping on asyncpg URLs
alembic>=1.13,<1.15
pydantic>=2.7,<3
pydantic-settings>=2.4,<3
redis>=5.0,<6
httpx>=0.27,<0.29
python-jose[cryptography]>=3.3,<4
cryptography>=42,<46  # Fernet encryption for env vars (step-62)
passlib[bcrypt]>=1.7.4,<2
opentelemetry-api>=1.27,<2
opentelemetry-sdk>=1.27,<2
opentelemetry-instrumentation-fastapi>=0.48b0
opentelemetry-instrumentation-sqlalchemy>=0.48b0
opentelemetry-exporter-otlp>=1.27,<2
opentelemetry-semantic-conventions>=0.48b0
pexpect>=4.9,<5
python-multipart>=0.0.9
websockets>=13.0,<14
structlog>=24.1,<25
litellm>=1.40,<2   # client SDK ONLY for type stubs; HTTP via httpx in production
jinja2>=3.1,<4     # step-78 F11 prompt template rendering (used by prompts service)

# LangGraph SDLC agent (Phase 5)
langgraph>=0.2.0
langchain>=0.3.0
langchain-core>=0.3.0

# Steering Rules Engine (F-504): filesystem watcher for live re-indexing
watchdog>=4.0,<7
