# forge-pi-score-idea <idea-id>

> Runs the forge-pi scorer against a specific idea and returns the
> `IdeaScore` (score + chain-of-thought trace + verdict).

## Usage

```
/forge-pi-score-idea idea_abc123
```