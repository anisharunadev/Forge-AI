{
  "name": "@fora/cache-broker",
  "version": "0.1.0",
  "description": "FORA cache broker with tenant-tagged keys. Every key is sha256(tenant_id + ':' + resource + ':' + id); the broker refuses any cross-tenant get/set and emits a canonical `tenancy.denied` audit event on mismatch. See FORA-124 acceptance bar #4 and FORA-165.",
  "license": "UNLICENSED",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "^22.5.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
