# Migration plan `mig-6391eb0ad0e3`

- **Schema version:** `1`
- **Planner version:** `migration-planner/0.1.0`
- **Wave plan id:** `93b5c8e4-0da0-483c-abfb-d561569df0fc`
- **Repo fingerprint:** `c6cac1a26669eddf`
- **Plan SHA:** `sha256:fca50b854…`
- **Source SHA:** `sha256:29e2035f2…`
- **Stories:** 8 (waves=7, cycle_breaks=0, cluster_breaks=0)
- **Total effort:** 5.1 d
- **Deterministic:** `True`
- **Cost (USD):** `0.00`

## Epic

### [Refactor] Modernize `c6cac1a26669eddf` (7 waves, 18 files)

## Why

Modernize `c6cac1a26669eddf` via the AWS Transform plan
emitted by [8.3](#) (wave plan `93b5c8e4-0da0-483c-abfb-d561569df0fc`).

## Scope

- **Waves:** 7 (preflight + cutover + validation included)
- **Files in scope:** 18 (skip-tier files excluded)
- **Cycle breaks:** 0
- **Cluster breaks:** 0
- **High-risk waves:** 6
- **Total estimated effort:** 5.1 d

## How

This epic is generated by the Forge AI migration planner (8.4) from the
`wave-plan.json` artefact emitted by 8.3. Each child story is
deterministically derived from a single TransformWave or WaveBreak
and carries a stable `idempotency_key` so re-runs do not create
duplicates.

## Board gate

The first child story is the preflight wave; downstream Dev work
(Epic 3) is gated on the `request_confirmation` the migration
planner opens on Forge AI-85 after this epic lands in the sprint.

## Stories (in execution order)

### 1. [Refactor] Modernize `c6cac1a26669eddf` (7 waves, 18 files)

- **Kind:** `epic`  |  **Priority:** `highest`  |  **Effort:** 0.0 d  |  **Sprint:** unassigned
- **Idempotency key:** `fora:8.4:epic:sha256:fca50b85400635e432d485c8f`
- **Links:** `aws_transform_plan` -> forge/8.3/wave-plan.json#93b5c8e4-0da0-483c-abfb-d561569df0fc, `dependency_graph` -> forge/8.2/dep-graph.json#93b5c8e4-0da0-483c-abfb-d561569df0fc

## Why

Modernize `c6cac1a26669eddf` via the AWS Transform plan
emitted by [8.3](#) (wave plan `93b5c8e4-0da0-483c-abfb-d561569df0fc`).

## Scope

- **Waves:** 7 (preflight + cutover + validation included)
- **Files in scope:** 18 (skip-tier files excluded)
- **Cycle breaks:** 0
- **Cluster breaks:** 0
- **High-risk waves:** 6
- **Total estimated effort:** 5.1 d

## How

This epic is generated by the Forge AI migration planner (8.4) from the
`wave-plan.json` artefact emitted by 8.3. Each child story is
deterministically derived from a single TransformWave or WaveBreak
and carries a stable `idempotency_key` so re-runs do not create
duplicates.

## Board gate

The first child story is the preflight wave; downstream Dev work
(Epic 3) is gated on the `request_confirmation` the migration
planner opens on Forge AI-85 after this epic lands in the sprint.

### 2. Wave 0 — `preflight` (preflight)

- **Kind:** `wave`  |  **Priority:** `high`  |  **Effort:** 0.0 d  |  **Sprint:** unassigned
- **Idempotency key:** `fora:8.4:wave:sha256:fca50b85400635e43:w0`
- **Source wave:** `0`
- **Links:** `wave_plan` -> forge/8.3/wave-plan.json#wave-0
- **Acceptance criteria:**
  - Build compiles (no new warnings introduced).
  - Smoke tests pass (no regression in adjacent modules).
  - Lint is clean (no new rule violations).
  - Audit log entry recorded for the wave's `audit_action`.
  - Canary probe green (Forge AI-194).
  - Secret rotation check green (Forge AI-128).

## Why

Pre-flight: probe tenant credentials, canary MGN reachability, verify secret rotation, register the repo with AWS Migration Hub.

## Prerequisites

_none_

## AWS services touched

- `migrationhub`
- `secretsmanager`

## Files in scope (0)

_none — infra-only wave_

## Gates

- `canary_probe` (Probe tenant credentials + MGN reachability before any transform wave fires. Implementation: customer-cloud-broker probe-signer (Forge AI-194)., blocking=True)
- `secret_rotate_check` (Verify every per-platform credential is in the AWS Secrets Manager inventory and within its rotation window. Implementation: mcp-servers/secrets (Forge AI-128)., blocking=True)

## Commands (seam-routed, not executed by this story)

- `migrationhub.MigrationHub.create_application_component` via `customer-cloud-broker/audit` (audit: `aws.migrationhub.create_component`)

## Effort

`0.0` d

### 3. Wave 1 — T1 `<unassigned>` (tier-T1-<unassigned>)

- **Kind:** `wave`  |  **Priority:** `high`  |  **Effort:** 1.7 d  |  **Sprint:** unassigned
- **Idempotency key:** `fora:8.4:wave:sha256:fca50b85400635e43:w1`
- **Source wave:** `1`
- **Links:** `wave_plan` -> forge/8.3/wave-plan.json#wave-1
- **Acceptance criteria:**
  - Build compiles (no new warnings introduced).
  - Smoke tests pass (no regression in adjacent modules).
  - Lint is clean (no new rule violations).
  - Audit log entry recorded for the wave's `audit_action`.
  - Canary probe green (Forge AI-194).
  - Secret rotation check green (Forge AI-128).
  - Audit log has the `transform.*` events for this wave.

## Why

Migrate 5 T1 ec2 file(s) in service `<unassigned>` via AWS mgn, ec2, migrationhub.

## Prerequisites

_none_

## AWS services touched

- `mgn`
- `ec2`
- `migrationhub`

## Files in scope (5)

- `src/main/java/com/billingcorp/jobs/MonthlyRollup.java`
- `src/main/java/com/billingcorp/util/weblogic/BillingBridge.java`
- `web/app.js`
- `web/index.html`
- `zzz_legacy/old_reporting/ReportAggregator.java`

## Gates

- `canary_probe` (High-risk T1/<unassigned> wave; canary-probe before any MGN/DMS/Refactor Spaces action fires., blocking=True)
- `secret_rotate_check` (Pre-flight secret rotation check for `ec2` credentials., blocking=True)
- `audit_completeness_check` (Post-wave audit completeness check for T1/<unassigned>., blocking=True)

## Commands (seam-routed, not executed by this story)

- `ec2.MGN.start_replication` via `customer-cloud-broker/dispatch:ec2` (audit: `aws.mgn.create`)

## Effort

`1.7` d

### 4. Wave 2 — T2 `<unassigned>` (tier-T2-<unassigned>)

- **Kind:** `wave`  |  **Priority:** `high`  |  **Effort:** 0.4 d  |  **Sprint:** unassigned
- **Idempotency key:** `fora:8.4:wave:sha256:fca50b85400635e43:w2`
- **Source wave:** `2`
- **Links:** `wave_plan` -> forge/8.3/wave-plan.json#wave-2
- **Acceptance criteria:**
  - Build compiles (no new warnings introduced).
  - Smoke tests pass (no regression in adjacent modules).
  - Lint is clean (no new rule violations).
  - Audit log entry recorded for the wave's `audit_action`.
  - Canary probe green (Forge AI-194).
  - Secret rotation check green (Forge AI-128).
  - Audit log has the `transform.*` events for this wave.

## Why

Migrate 4 T2 lambda file(s) in service `<unassigned>` via AWS lambda, apigateway.

## Prerequisites

_none_

## AWS services touched

- `lambda`
- `apigateway`

## Files in scope (4)

- `lambdas/notify_customer.py`
- `src/main/java/com/billingcorp/models/rds/LegacyInvoiceRow.java`
- `src/main/java/com/billingcorp/monolith/Customer.java`
- `src/main/java/com/billingcorp/monolith/Invoice.java`

## Gates

- `canary_probe` (High-risk T2/<unassigned> wave; canary-probe before any MGN/DMS/Refactor Spaces action fires., blocking=True)
- `secret_rotate_check` (Pre-flight secret rotation check for `lambda` credentials., blocking=True)
- `audit_completeness_check` (Post-wave audit completeness check for T2/<unassigned>., blocking=True)

## Commands (seam-routed, not executed by this story)

- `lambda.Lambda.create_function` via `customer-cloud-broker/dispatch:lambda` (audit: `aws.lambda.create`)

## Effort

`0.4` d

### 5. Wave 3 — T3 `<unassigned>` (tier-T3-<unassigned>)

- **Kind:** `wave`  |  **Priority:** `high`  |  **Effort:** 1.3 d  |  **Sprint:** unassigned
- **Idempotency key:** `fora:8.4:wave:sha256:fca50b85400635e43:w3`
- **Source wave:** `3`
- **Links:** `wave_plan` -> forge/8.3/wave-plan.json#wave-3
- **Acceptance criteria:**
  - Build compiles (no new warnings introduced).
  - Smoke tests pass (no regression in adjacent modules).
  - Lint is clean (no new rule violations).
  - Audit log entry recorded for the wave's `audit_action`.
  - Canary probe green (Forge AI-194).
  - Secret rotation check green (Forge AI-128).
  - Audit log has the `transform.*` events for this wave.

## Why

Migrate 6 T3 api_gateway file(s) in service `<unassigned>` via AWS apigateway, lambda.

## Prerequisites

_none_

## AWS services touched

- `apigateway`
- `lambda`

## Files in scope (6)

- `src/main/java/com/billingcorp/api/BillingController.java`
- `src/main/java/com/billingcorp/monolith/ReportsService.java`
- `src/main/java/com/billingcorp/pipeline/InvoicePipeline.java`
- `src/main/java/com/billingcorp/util/LegacyDb.java`
- `src/main/java/com/billingcorp/util/UnusedHelper.java`
- `target/classes/com/billingcorp/monolith/BillingService.class`

## Gates

- `canary_probe` (High-risk T3/<unassigned> wave; canary-probe before any MGN/DMS/Refactor Spaces action fires., blocking=True)
- `secret_rotate_check` (Pre-flight secret rotation check for `api_gateway` credentials., blocking=True)
- `audit_completeness_check` (Post-wave audit completeness check for T3/<unassigned>., blocking=True)

## Commands (seam-routed, not executed by this story)

- `api_gateway.APIGateway.create_api` via `customer-cloud-broker/dispatch:apigateway` (audit: `aws.apigateway.create`)

## Effort

`1.3` d

### 6. Wave 4 — T4 `<unassigned>` (tier-T4-<unassigned>)

- **Kind:** `wave`  |  **Priority:** `high`  |  **Effort:** 1.2 d  |  **Sprint:** unassigned
- **Idempotency key:** `fora:8.4:wave:sha256:fca50b85400635e43:w4`
- **Source wave:** `4`
- **Links:** `wave_plan` -> forge/8.3/wave-plan.json#wave-4
- **Acceptance criteria:**
  - Build compiles (no new warnings introduced).
  - Smoke tests pass (no regression in adjacent modules).
  - Lint is clean (no new rule violations).
  - Audit log entry recorded for the wave's `audit_action`.
  - Canary probe green (Forge AI-194).
  - Secret rotation check green (Forge AI-128).
  - Audit log has the `transform.*` events for this wave.

## Why

Migrate 1 T4 container file(s) in service `<unassigned>` via AWS ecs, fargate, ecr.

## Prerequisites

_none_

## AWS services touched

- `ecs`
- `fargate`
- `ecr`

## Files in scope (1)

- `src/main/java/com/billingcorp/monolith/BillingService.java`

## Gates

- `canary_probe` (High-risk T4/<unassigned> wave; canary-probe before any MGN/DMS/Refactor Spaces action fires., blocking=True)
- `secret_rotate_check` (Pre-flight secret rotation check for `container` credentials., blocking=True)
- `audit_completeness_check` (Post-wave audit completeness check for T4/<unassigned>., blocking=True)

## Commands (seam-routed, not executed by this story)

- `container.ECS.create_service` via `customer-cloud-broker/dispatch:ecs` (audit: `aws.ecs.create`)

## Effort

`1.2` d

### 7. Wave 5 — `cutover` (cutover)

- **Kind:** `wave`  |  **Priority:** `high`  |  **Effort:** 0.2 d  |  **Sprint:** unassigned
- **Idempotency key:** `fora:8.4:wave:sha256:fca50b85400635e43:w5`
- **Source wave:** `5`
- **Links:** `wave_plan` -> forge/8.3/wave-plan.json#wave-5
- **Acceptance criteria:**
  - Build compiles (no new warnings introduced).
  - Smoke tests pass (no regression in adjacent modules).
  - Lint is clean (no new rule violations).
  - Audit log entry recorded for the wave's `audit_action`.
  - Canary probe green (Forge AI-194).
  - Audit log has the `transform.*` events for this wave.

## Why

Cutover: flip DNS / routing to point at the migrated AWS footprint. Refuses to flip if any canary-probe fails.

## Prerequisites

_none_

## AWS services touched

- `route53`
- `cloudfront`
- `migrationhub`

## Files in scope (0)

_none — infra-only wave_

## Gates

- `canary_probe` (Canary-probe every cutover candidate route before DNS flip; refuse to flip if any route is unhealthy., blocking=True)
- `audit_completeness_check` (Pre-cutover audit completeness check across every completed wave., blocking=True)

## Commands (seam-routed, not executed by this story)

- `route53.Route53.change_resource_record_sets` via `customer-cloud-broker/dispatch:route53` (audit: `aws.route53.cutover`)

## Effort

`0.2` d

### 8. Wave 6 — `validation` (validation)

- **Kind:** `wave`  |  **Priority:** `low`  |  **Effort:** 0.2 d  |  **Sprint:** unassigned
- **Idempotency key:** `fora:8.4:wave:sha256:fca50b85400635e43:w6`
- **Source wave:** `6`
- **Links:** `wave_plan` -> forge/8.3/wave-plan.json#wave-6
- **Acceptance criteria:**
  - Build compiles (no new warnings introduced).
  - Smoke tests pass (no regression in adjacent modules).
  - Lint is clean (no new rule violations).
  - Audit log entry recorded for the wave's `audit_action`.
  - Audit log has the `transform.*` events for this wave.

## Why

Validation: smoke test + synthetic canary against the cutover footprint. Refuses to close if either fails.

## Prerequisites

_none_

## AWS services touched

- `cloudwatch`
- `synthetics`
- `migrationhub`

## Files in scope (0)

_none — infra-only wave_

## Gates

- `unit_test` (Run the project's smoke test against the cutover footprint., blocking=True)
- `audit_completeness_check` (Post-cutover audit completeness check; ensures every wave emitted its audit events., blocking=True)

## Commands (seam-routed, not executed by this story)

- `cloudwatch.Synthetics.create_canary` via `customer-cloud-broker/audit` (audit: `aws.synthetics.canary_create`)

## Effort

`0.2` d

