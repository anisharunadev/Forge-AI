{
  "name": "@fora/db-migrator",
  "version": "0.1.0",
  "description": "FORA Postgres migration runner — single entry point that bootstraps every multi-tenant table with the canonical tenant_isolation RLS policy, enforces a BYPASSRLS audit, and ships the property-based test that proves cross-tenant reads are impossible. Implements FORA-162 (0.7.2a).",
  "license": "UNLICENSED",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "migrations",
    "audit",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "migrate": "node bin/fora-db-migrate.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "pg": "^8.13.0"
  },
  "devDependencies": {
    "@types/node": "^22.5.0",
    "@types/pg": "^8.11.10",
    "fast-check": "^3.23.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
