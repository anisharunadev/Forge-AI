{
  "name": "@fora/agent-runtime",
  "version": "0.2.3",
  "description": "FORA Agent Runtime — stage machine, allow-list gateway, run record writer, retry/idempotency/budget/cancel. See FORA-144 + FORA-145.",
  "license": "UNLICENSED",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "fora-agent-runtime-smoke": "bin/fora-agent-runtime-smoke.mjs"
  },
  "files": [
    "dist",
    "bin",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "smoke": "node bin/fora-agent-runtime-smoke.mjs",
    "dev": "node bin/fora-agent-runtime-dev.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "^22.5.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
